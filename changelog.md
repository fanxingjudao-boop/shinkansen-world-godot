# Changelog

verification-agent LIGHT モードで Claude Code が変更を記録します。

## v0.57.0 — 2026-06-06 — 主人公を えらべる(うんてんしさん / きつね)

改善さんリクエスト「キャラクターを選べる形に」。`docs/design_handoff_fox_character/` のキツネを追加し、タイトルで主人公を選べるようにした。

- **選べる主人公の枠組み**(新規 `scripts/entities/characters/`): `character_visual.gd`(見た目の基底クラス=パーツ生成ヘルパー+`build()`/`animate_walk()`/`wave()`/`celebrate()` の口)、`driver_character.gd`(従来の うんてんしさんを切り出し・見た目/寸法そのまま)、`fox_character.gd`(キツネ・新規)、`character_roster.gd`(名簿レジストリ)。**新しい主人公を増やすときは 基底を継承したスクリプト+名簿に1行 でよい**(player/title は触らない)。
- **キツネ**(`fox_character.gd`): handoff の Three.js モデル(`_buildModel`)を Godot プリミティブで忠実再現。オレンジの体・大きな白いむね・**段重ねの長い耳**(さびオレンジ→オレンジ→ピーチ→白→暗い先)・**赤いスカーフ**・**うるうるアンバーの目**(アーモンド枠+虹彩+大きな瞳+キラキラ)・ひげ・**巨大なふさふさ尻尾**(オレンジ→クリーム→白)。歩くと前足がゆれ・尻尾と耳がゆれ・ときどき まばたき。元モデルは +Z 向き全高~3.4 なので 内部グループを 180°回転(顔=-Z に統一)+`MODEL_SCALE=0.55` でうんてんしさんと近い背丈に。
- **player.gd の改修**: 移動・物理だけ残し、見た目とアニメ(歩行/てをふる/よろこび)を 選択中の主人公(`character_visual.gd` の子孫)へ委譲。`set_character(id)` で 古い見た目を消して 新しく生成し GameState に保存。`_ready` で セーブ済み(既定 driver)の主人公を生成。
- **タイトルで選ぶ**(`title.gd`): 「だれで あそぶ?」+ 主人公の絵カード(名簿から自動生成・イメージ色・タッチターゲット 240×170)。タップで `Player.set_character` を呼び **その場で切替**(背景が半透明なので 後ろの主人公が切り替わるのが見える)。選択中は白い太枠+ぷにっと拡大。
- **保存**: `game_state.gd` に `selected_character`(英数字 id・氏名等の個人情報ではない)+`set_character()`、`save_system.gd` で `"character"` を永続化。次回も同じ主人公で始まる。

検証: 全スクリプト構文チェック通過、`AUTO_CHARSEL`(新規)で うんてんしさん/きつね を切り替えて接写・GameState 保存(driver→fox)を確認、Main 本体クリーン起動・実行時エラーなし、`export_presets.cfg` 無変更・MODE は SINGLE に復帰・save.json 削除。**改善さんの体感確認待ち**: キツネの見た目(大きさ `MODEL_SCALE`・耳/尻尾の可愛さ・怖くないか)、タイトルの選ぶ導線、切替の分かりやすさ。**調整候補**: `fox_character.gd MODEL_SCALE`、`character_roster.gd` の名前(`きつねさん`)/色、タイトルのカード寸法。

## v0.56.1 — 2026-06-06 — 踏切バグ修正 + 電車が さらに自由に行き来

改善さんフィードバック「踏切が 電車が通る前に閉まる」「環状で遅い・すべての電車が自由に行き来できるように」。

- **踏切バグ修正**(`crossing.gd`): 原因は ①接近判定が `get_slug()`(編成の"正体"・分岐しても不変)で 編成を探していた → v0.55.0 で komachi 等を分岐対象にした結果、**載せ替え後に「その線路にいない編成」を追ってしまう**。②**直線距離22m**判定なので 小さな環状では 手前の弧でも 近くなり「通る前に閉まる」。→ **`get_route_slug()`(実際にその線路を走っている編成)で追い、弧長(線路に沿った前方距離 `CLOSE_AHEAD=16m`/通過後 `PASS_CLEAR=7m`)で判定**するように修正。載せ替えで編成が変わっても 正しく追従。
- **電車が 自由に行き来**(`route_data.gd branches()`): 本線はやぶさをハブにした各地分岐に **doctor_yellow・tsubame(高架)** を追加。これで **全地上ルート(みずうみ/やま/まち/おしろ/まちのまんなか/ドクターイエロー/つばめ高架/本線3)へ 運転で行き来できる**。`AUTO_FIXCHECK` で 重複なし(1ルート1編成 維持)を確認。
- 「環状で遅い」について: 分岐で 各地へ走れるようになったので「ぐるぐる回るだけ」感は 解消方向。さらに速くしたい場合は `resources/train_data/*.tres` の `speed` で調整可能(今回は据え置き)。

検証: 構文チェック通過、`AUTO_FIXCHECK`(各地分岐 重複なし)・`AUTO_CROSSING`(踏切の描画・開閉)で確認、実行時エラーなし。**踏切の開閉タイミングは 実機での体感確認推奨**(静止スクショでは タイミングまでは確認しきれない)。**調整候補**: `crossing.gd CLOSE_AHEAD/PASS_CLEAR`、各地分岐の `at_ratio`/数、`train_data.speed`。

## v0.56.0 — 2026-06-06 — ミニマップ(ちず)で 迷わない

改善さんフィードバック「地図がないので迷いそう。見やすく かつ 視界の邪魔にならないように」。新規 `scripts/ui/minimap.gd`(`main.gd` が `_spawn_extra` で生成・`Main.tscn`/`TouchHUD.tscn` 不変)。

- **右上のすみ**に 小さい(約152px)・**半透明**(うっすら)・角丸のミニマップ。中央のあそび場と かぶらない。**北が上の固定地図**。
- 表示物: 主人公(**向きのわかる白い矢印**)/ 湖(青)/ 山×3(灰の三角)/ 街×4(オレンジ四角)/ お城(桃のひし形)/ 北の目印。座標は `terrain_height.gd`・`town.gd` と一致。
- **視界・操作の邪魔をしない**: `mouse_filter=IGNORE`(タップすり抜け)、0.12秒ごと更新(軽い)、**別世界(つき/うみ/ぎんが等=遠い/高い位置)や 電車に乗っている間・図鑑/メニュー(paused)中は 自動で隠す**。
- 併せて `main.gd` の `_spawn_extra` を `Node3D` 固定から **任意 Node 対応**に(CanvasLayer のミニマップを 生成できるように。既存の Node3D extra も問題なし)。

検証: 構文チェック通過、地上で **右上にミニマップが出る**ことをスクショ確認(別世界/乗車/モーダル中は隠す)、実行時エラーなし。**改善さんの体感確認待ち**: 位置・大きさ・透明度・目印の見やすさ。**調整候補**: `MAP_SIZE`/`MAP_RANGE`/位置(offset)/色。

## v0.55.0 — 2026-06-05 — 本格的な線路・踏切・道路 + 電車で各地へ移動

改善さんリクエスト「①電車を各地に移動 ②本格的な踏切 ③街をつなぐ道路 ④本格的な線路」を一括実装。

- **④本格的な線路**(`railway.gd`): 各ルートに **砂利のバラスト道床**(`_build_ballast_for`・ArrayMesh 1枚/ルート)を追加。**枕木を密に**(`TIE_SPACING` 3.8→1.8m・実物寄りサイズ)。レールを **鋼のてかり**(metallic 0.6→0.8・roughness 0.4→0.28)。AUTO_RIDE で確認。
- **②本格的な踏切 増設**(`town.gd` `CROSSINGS`): 既存の本格踏切(遮断機・警報灯・警報音・接近検知=`crossing.gd`)を **3→8 か所**に(komachi/e235_yamanote/doctor_yellow の地表ルート)。
- **③街をつなぐ道路**(`town.gd` `_build_roads`/`_road_ribbon`): メインの街↔各集落 を結ぶ **道路3本**(地形に沿う帯メッシュ ArrayMesh+まんなかの白線)。道路を先に敷いてから建物を配置。BIRD 俯瞰で確認。
- **①電車で各地へ移動**(`route_data.gd` `branches()` + `ride_controller.gd`): `take_branch` は **フェード中点でルート載せ替え+編成入れ替え**するので、離れたルートでも飛びは見えない。これを使い **本線(はやぶさ)をハブに みずうみ/やま/まち/おしろ/まちのまんなか への往復分岐**を追加。運転中に2択で行き先を選ぶと その名所のループへ。`label`(子供向け地名)を HUD 2択・通知に表示。`AUTO_FIXCHECK` で 本線→みずうみ の遠距離スワップを検証(**重複なし=1ルート1編成 維持・編成入れ替え成立**)。

検証: 全スクリプト構文チェック通過、AUTO_RIDE(線路)/BIRD(道路)/AUTO_FIXCHECK(各地分岐)で確認、実行時エラーなし。MODE/VIEW は復帰・save.json 削除。**改善さんの体感確認待ち**: 線路の見た目(枕木の密度=性能 `TIE_SPACING`)、踏切の数・位置、道路の通り道(地形/水と重ならないか)、**各地分岐の運転体験(2択の出る場所 `at_ratio`・行き先・酔わないか)は実機での調整が要**。
- **調整候補**: `railway.gd TIE_SPACING/BALLAST_WIDTH`、`town.gd CROSSINGS/ROADS/ROAD_WIDTH`、`route_data.gd branches()` の各地分岐 at_ratio/to_ratio・label。

## v0.54.2 — 2026-06-05 — 銀河鉄道が 6つの世界を めぐる旅に

改善さんフィードバック「銀河鉄道は そら・ゆき・お菓子の国・月・恐竜の場所に行ったりして」。旅の止まり先(12)を、大きな星 6 と **6つの世界のミニチュア(浮かぶ島)** に変更。汽車が 星と世界を 次々に渡って いろんな所をめぐる。

- **各世界の止まり先**(`ginga_railway.gd` に追加): **そら**(雲の上のおしろ)/**ゆき**(ゆきだるま+もみの木)/**おかし**(ロリポップ+カップケーキ)/**つき**(クレーターの月)/**きょうりゅう**(やさしい くびなが恐竜)/**うみ**(水の島+くじら)。それぞれ 浮かぶ島の上に作り、**ひらがなの名前ラベル**(カメラを向く Label3D)を添える。
- 旅路の並びは 星→そら→星→ゆき→星→おかし→星→つき→星→きょうりゅう→星→うみ(`_build_journey` の `kinds`)。大きな星(`_build_big_star`)・レール・星のかけら・うねる立体ルートは v0.54.1 のまま。
- 既存の各ワールド本体(yuki_land 等)は不変。これは銀河の中の「ミニチュア」で、本物のワールドは従来どおり乗り物/おでかけメニューから。

検証: 構文チェック通過、`AUTO_GINGA` で **ゆきだるま・恐竜の島・くじらの島などが星の間に浮かび 汽車がめぐる**ことをスクショ確認(名前ラベル表示)、実行時エラーなし。MODE は SINGLE 復帰・save.json 削除。**改善さんの体感確認待ち**: 各ミニチュアの見え方・大きさ・ラベルの読みやすさ・並び順。

## v0.54.1 — 2026-06-05 — 銀河鉄道を「星々を渡り歩く」長い旅路に作り替え

改善さんフィードバック「銀河はすべての星々を渡り歩くようなイメージで」。`ginga_railway.gd` の巡航を、小さな1つの輪の周回から **たくさんの大きな星のあいだを 上下にうねりながら 次々に渡っていく 長い旅路** に変更。

- **旅路**(新 `_build_journey`): 大きな星 12 個を 大きな輪(半径 `GALAXY_R=95`・半径を sin で ゆらす)に並べ、`STAR_WAVE_Y=22` で **上下にうねる立体ルート**に。汽車は 各星の すぐそばを 次々に通り過ぎる(渡り歩く)。巡航速度 4.5→**9.0**(ぐんぐん渡る)。
- **大きな星**(新 `_build_big_star`): いろいろな色 8 種(すべての星々)の 光る玉 + うすい光のかさ(halo)+ ゆっくり またたく。
- **光るレール**: 星と星を つなぐ 点の連なり(MultiMesh で 1 draw call)。星のかけら(ごほうび)は 旅路の区間の まんなかに配置。
- 惑星(土星ふくむ)は 旅路の外の 遠景へ移動(通り道と重ならない)。星空・天の川・月・ランタンは そのまま。

検証: 構文チェック通過、`AUTO_GINGA` で **色とりどりの大きな星の間を 汽車がうねって渡る**ことをスクショ確認(`screenshot_ginga.png`/`_wide.png`)、実行時エラーなし。MODE は SINGLE 復帰・save.json 削除。**改善さんの体感確認待ち**: 旅の長さ・速度・星の数/色/間隔・うねりの大きさ(酔わないか)。

## v0.54.0 — 2026-06-05 — 第9の目玉: 銀河鉄道(星空を走る夢の汽車)

改善さんリクエスト。**星空・天の川を 夢の汽車で めぐる別世界**(銀河鉄道の夜 / 999 風)。submarine/yuki と同じ「乗り物→ワープ+自動巡航」骨格だが、**`Main.tscn`/`TouchHUD.tscn` を一切編集せず**に追加(寄生方式)。

- **新規 `scripts/world/ginga_railway.gd`**(`main.gd` が `_spawn_extra` で生成): 草原の夢の汽車に近づくと HUD ボタン→フェード→`GINGA_POS(3200,500,3200)` の星空へ。プレイヤーを汽車に装着し physics off+gravity0 で **光るレールの輪(TorusMesh)を自動巡航**。**HUD ボタン(GingaButton)もスクリプトが実行時に生成**(TouchHUD.tscn 不変・他ワールドと同じ上部中央スロット・ぎんが色)。
- **見た目**: 銀河鉄道999 風の機関車(ボイラー/えんとつ/運転室/排障器/動輪・金のふち・光る窓・ヘッドライト)+ 客車(光る窓3つ)。星 180個(MultiMesh 1 draw call)・天の川(うすい帯)・大きな月・惑星4つ(土星は輪つき)・ふわふわ光るランタン・光るレール。**怖くない明るい星空**(真っ黒にしない=深い藍+たくさんの星で ルミナス・ambient 0.6)。
- **ごほうび**: 星のかけら5個(近接でゲット→ほし+1・バースト+音)。「おうちへ かえる」常時。
- **統合**: `game_state.visited_ginga`+`set_ginga_visited`、`save_system` に "ginga"、`mission_manager` に「ぎんがてつどうで ほしぞらを はしろう」、**おでかけメニュー(`world_select.gd` WORLDS)に "ぎんが" カード追加**(7枚目)。env は他ワールドと同じ fog 退避/復元。
- 子供配慮: 自動運転で安心・落下/失敗なし・急に出てこない・やさしい音・暗すぎない。セキュリティ原則維持。

検証: 7 スクリプト構文チェック通過、新規 `AUTO_GINGA` で `warp_in`→`ginga active: true`・**汽車が光るレールの輪を巡航/星・惑星が描画**を確認(`screenshot_ginga.png`/`_wide.png`)、`AUTO_WORLDSEL` で**メニュー7枚目「ぎんが」表示**を確認、実行時エラーなし。`Main.tscn` 不変(load_steps 据え置き)・`for_mobile=false`・MODE は SINGLE 復帰・save.json 削除。**改善さんの体感確認待ち**: 星空の明るさ・色(もっと宇宙っぽく暗くするか)、汽車の見やすさ、巡航速度、地上ドック位置(56,24)、**おでかけメニュー7枚目が画面下に近い→パネル高さ微調整候補**。

## v0.53.0 — 2026-06-05 — 遊び心: かくれた でんしゃ(レア車両・夜)(B-7)

`docs/PLAYFUL_DETAILS.md` の B-7。**夜だけ にじいろの「ゆめ」しんかんせんが線路に現れる**(§12 でも本命だった隠し電車)。図鑑の隠し枠で、見つけて乗ると うまる。`Main.tscn` 不変・**セーブ項目を増やさない**(発見=乗車=既存の `boarded_trains` を流用)。

- **専用ルート**(`route_data.gd` に `"yume"` を追加): 中ループ(center(0,0)・rx120/rz100・elevation0)。他編成と別 Path3D なので衝突しない。線路は常設・列車は夜だけ走る。**位置は実機で調整可**。
- **車両データ**(新規 `resources/train_data/yume.tres`): 表示名「ゆめ」・にじいろ(body=ラベンダー/accent=金)・rounded ノーズ・top_speed 999。図鑑は `.tres` 自動走査なので**追加だけで「?」の隠し枠として出る**(`book.gd` 改変不要)。
- **出現制御**(新規 `scripts/world/rare_train.gd`、`main.gd` が `_spawn_extra` で生成): `time_of_day < 0.22 or > 0.80`(ホタル/流れ星と同じ夜判定)で `Train.tscn` を instance して `Trains` に追加 → 乗車・図鑑・すれ違い手振りに自動波及。昼になると `queue_free`(ただし**乗車中は降りるまで消さない**)。初めて乗ると「ゆめの でんしゃ みつけた!」通知+ほし+1。
- 子供配慮: 夜になれば必ず出る(優しい条件)・怖くない・見つけられなくても損しない。

検証: 4 スクリプト構文チェック通過、新規 `AUTO_RARE`(夜に強制→出現待ち→撮影)で **`rare spawned: true`・ゆめ電車が夜の線路に5両編成で描画**されることを確認(`screenshot_rare_train.png`)、実行時エラーなし(`for_mobile=false` 維持・MODE は SINGLE 復帰・save.json 削除)。**改善さんの体感確認待ち**: ルート位置(街/他線/地形と重ならないか)、夜の見やすさ・色、乗りやすさ。**調整候補**: `route_data.gd` の "yume" 位置/大きさ、`yume.tres` の色。

## v0.52.0 — 2026-06-05 — 遊び心: 楽器(押すと音が鳴る たからもの)(B-5)

`docs/PLAYFUL_DETAILS.md` の B-5。音を出す遊びは子供の根源的な楽しみ。新規 `scripts/world/instruments.gd`(`main.gd` が起動時に `_spawn_extra` で生成)。`Main.tscn` 不変・`load_steps` 据え置き・セーブ項目も増やさない。既存の B-2(`interactables.gd`)の物理ピッキングと sound_fx のプロシージャル音の作法を踏襲。

- **もっきん(主役)**: ドレミファソラシド(C5..C6)の **8 枚のにじいろ鍵盤**。各鍵盤が個別の `Area3D.input_event`(カメラからのタップ)で、押すと その音が鳴り **ぺこっと沈んで戻る**。素早く戻すので **自分でメロディが弾ける**。鍵盤は低音ほど長い本物の木琴風。木の台+レール付き。
- **ラッパ**: 金色のベル(コーン)+吹き口。タップで **ぴゃ〜と短いファンファーレ(ドミソド)** + のびあがる演出。
- 音は ひかえめ(`_sfx.volume_db=-10`・やわらかい正弦波・sin エンベロープ)。ミュートは Master バス(settings「おと」)一括なので 既定バス接続で自動連動。連打よけ `_busy`・壊れる/痛がる 表現なし。

検証: 3 スクリプト構文チェック通過、`Main.tscn` headless 起動エラーなし、新規 `AUTO_MUSIC` で **もっきん(にじいろ8鍵盤)・ラッパ(金のベル)が地上に正しく表示**されることをスクショ確認(`screenshot_music_xylophone.png`/`_trumpet.png`)。`for_mobile=false` 維持・MODE は SINGLE 復帰・save.json 削除。**改善さんの体感確認待ち**: 置き場所(`XYLO_POS`(12,8)/`TRUMPET_POS`(4,-12)が他構造物と重ならないか)、音色・音量、鍵盤の弾きやすさ。

## v0.51.2 — 2026-06-05 — 全機能監査 + 戻る機能の不統一を修正(月・空でいつでも帰れる)

全機能の完成度を3観点で監査(別世界の戻る機能 / 電車・乗車・ループ機構 / 収集・ミッション・HUD・遊び心・セーブ)。**実在した不足は1つ=「つき」「そらのおしろ」の戻るボタンが乗り物(ロケット/飛行機)の近接でしか出ない**(他4ワールド うみ/おかし/きょうりゅう/ゆき は常時表示)。月は歩ける惑星・空は歩ける雲島で探索できるため、乗り物から離れると帰れなくなる恐れがあった(=戻る機能が一部未実装)。

- **つき**(`moon_trip.gd`): `_update_moon_button()` の「おうちへ かえる」をロケット近接ゲートから **else(常時)** に変更。`_on_pressed()` もカー以外なら月のどこからでも `_go_home()`。カー(月面バギー)の のる/おりる は従来どおり優先。
- **そら**(`sky_castle.gd`): `_process` の戻るボタンを `near_home` 条件から **常時「おうちへ かえる」** に、`_on_pressed()` も飛行機近接ゲートを外して空のどこからでも `_go_home()`。

監査で他に挙がった候補はいずれも**実コード確認の結果 問題なし**(誤検知): candy は gravity/physics を一切変えないので復元不要 / submarine の `_player.rotation` は潜水中に変わらない(回るのは `_sub` のみ) / 空の `fog_light_color` は `day_night_cycle.gd:55` が毎更新で再設定するため復帰する / 線路の閉ループ・自動巡航ループ・park 編成・分岐・乗降・運転モードの抜けはすべて完結。HUD ボタン配線・収集→ごほうび・13ミッション到達性・セーブ往復・遊び心4種も配線漏れなし。

検証: `moon_trip.gd`/`sky_castle.gd` 構文チェック通過、`Main.tscn` headless 起動エラーなし、`AUTO_MOON` で月へワープ→歩行後も「おうちへ かえる」が常時表示されることをスクショで確認(`for_mobile=false` 維持・MODE は SINGLE 復帰・save.json 削除)。

## v0.51.1 — 2026-06-05 — 改善さんフィードバック: 乗客の「手(腕)」が怖い → 削除

A-5(v0.51.0)の手を振る乗客の **細い腕が幼児には怖い**との指摘(子供配慮=厳守)。**腕の見た目を完全に削除**し、挨拶は **「ぴょこっ」と上下バウンス**(`animal.wave_back` と同じやさしい動き)+ ♥ で表すように変更(`train.gd` の `_build_waver`/`wave_passengers`)。窓の中の乗客シルエット(顔なし・やわらかい色)・♥・すれ違い検知・距離カリングはそのまま。`AUTO_GREET` のスクショで **腕が消え、丸いシルエットだけになった**ことを確認。

## v0.51.0 — 2026-06-05 — 遊び心 第3弾: 電車の窓から乗客が手を振る(A-5)

`docs/PLAYFUL_DETAILS.md` の第3弾 A-5。走る電車の窓に乗客がいて、すれ違うと こちらへ手を振る。**乗車中は自分が振られる側になる**。「世界が生きている」感を出す。既存の窓 MultiMesh 最適化・A-1 の手振り語彙・寄生スポーン方式に乗せ、`Main.tscn` は不変・`load_steps`(64)据え置き・セーブ項目も増やさない。

- **乗客の見た目**(`train.gd` に `_attach_passengers`/`_build_waver` 追加): 窓の内側に乗客シルエット。大半は **車両ごと MultiMesh の丸い静止乗客**(1つおきの窓×両側=draw call ほぼ増えず・車輪と同じ 110m 距離カリングで遠景は非表示)。各車両に **1 人だけ「手を振る乗客」**(胴体+肩支点の腕の実ノード・`_wavers` に保持)。怖くないよう **顔は作らず、真っ黒も使わない**(編成ごとに やわらかいパステル 5 色をローテーション)。
- **手振り**(`train.gd::wave_passengers()` 公開): すれ違いざまに 腕を **上げて ふりふり2往復→下ろす**(`player.wave()` と同じバネ感)+ 窓から **♥** がひとつ ふわっと(`animal._pop_heart` 流用)。`_wave_cooldown`(3.5秒)で連発防止、カメラから 110m 超の編成は処理しない。
- **すれ違い検知**(新規 `scripts/world/train_greeters.gd`、`main.gd` が起動時に生成): 各 Train は他編成/Player を知らない疎結合のため、横断ロジックを 1 ノードに集約(animal_manager と同じ役割)。0.2 秒間隔で、観測者(乗車中=乗っている編成のアンカー/歩行中=プレイヤー)の **45m 以内** を通る編成に `wave_passengers()` を呼ぶ。乗っている編成自身は除外=自分が振られる側になる。`ride_controller` に `get_current_train()` を追加。

子供配慮: 手振りは ゆっくり・やさしく、♥ で温かさを添える。すれ違いは頻発しうるため **音は鳴らさない**(「やさしい音」厳守)。失敗概念なし。セキュリティ原則(外部通信なし・入力欄なし・PII なし・トラッキングなし)維持。

検証: 5 スクリプトの構文チェック通過、`AUTO_GREET`(新規)で AutoCapture を headless 起動 → **窓の中に乗客シルエットが見え、手振りで ♥ が出る** ことをスクショ(`screenshot_greet_ride.png`/`_ground.png`)で確認・実行時エラーなし(`for_mobile=false` 維持)。MODE は SINGLE に復帰済、AUTO で汚れた save.json は削除。**改善さんの PC ブラウザ/iPad 実機での 体感確認待ち**: 乗客シルエットの可愛さ・怖くなさ、手振りの速さ・頻度、すれ違い距離(`GREET_RANGE` 45m)、まどぎわ/うんてんせき視点での見え方、fps。

## v0.50.0 — 2026-06-04 — 遊び心 第2弾: きらきらふみいし(B-6) / くしゃみ・あくび(C-1) / たたくと反応(B-2)

`docs/PLAYFUL_DETAILS.md` の第2弾「軽くて効果的」3つ。いずれも Main 直下に小さなノードを 1 つ生成する方式(`main.gd` の `_spawn_extra`)で、`Main.tscn` は不変・`load_steps` 据え置き・セーブ項目も増やさない。

- **B-6 きらきらふみいし**(新規 `scripts/fx/magic_steps.gd`): 地面の 7 か所に うっすら光る目印。踏む(1.9m)と **ぱっと明るくふくらみ 花/星が ぽぽっと舞い上がる**。クールダウン 1.6 秒で **何度でも光る**(歩くこと自体が楽しい)。セーブなし。
- **C-1 くしゃみ・あくび・うとうと**(`animal.gd`): 立ち止まっている動物が 9〜18 秒ごとに ランダムで あくび(ぐーん+「ふぁ〜」)/くしゃみ(ぴくっ+「くしゅん」)/うとうと(ゆっくり上下+「Zzz」)。やさしい 3D 小音量ボイス(`AudioStreamPlayer3D`・22m 減衰)。`_celebrating` で 移動・歌・なかよしと競合させない。
- **B-2 たたくと反応するもの**(新規 `scripts/world/interactables.gd`): きのこ(ぽよん)/たいこ(ぽん)/ベル(ちりん)を **タップすると 反応**。`Area3D.input_event` + ビューポートの `physics_object_picking` を ON にして カメラからの タップを拾う(他システムは近接式なので影響なし)。連打よけ・ひかえめな音。壊れる/痛がる 表現なし。

(A-3「足元の鳥・蝶」は 改善さんの判断で 見送り。)

子供配慮: 反応は すべて やさしく(明るい音・ゆっくり・暗くしない)、失敗概念なし。セキュリティ原則(外部通信なし・入力欄なし・PII なし・トラッキングなし)維持。

検証: スクリプト構文チェック通過、`Main.tscn` を headless 起動して 実行時エラーなしを確認(`for_mobile=false` 維持)。**改善さんの PC ブラウザ/iPad 実機での 体感確認待ち**: ふみいしの位置・頻度、動物の小しぐさの可愛さ・音量、**B-2 のタップが Web/iPad で確実に効くか**(物理ピッキングの実機挙動・HUD ボタンとの干渉)。

## v0.49.0 — 2026-06-04 — 遊び心 第1弾: てをふると みんなが応える(A-1) / かくれんぼ動物(B-1)

`docs/PLAYFUL_DETAILS.md` の第1弾(目玉2つ)を実装。子供がいちばん うれしい「自分のしたことで世界が応えた」(反応系)と「自分だけが見つけた」(発見系)を 1 つずつ。どちらも 既存システムに寄生し、新しい巨大マネージャは作らない方針。

- **A-1 てをふると みんなが応える**(`player.gd`/`animal.gd`/`animal_manager.gd`/`touch_hud.gd`+`TouchHUD.tscn`): HUD に「てをふる」ボタン(ピンク・`Nagame` と同じ位置で 乗車中は非表示)。押すと 主人公が **右腕を上げて ふりふり**(`player.wave()`、`_waving` 中は歩行アニメが右腕を触らず競合回避)。`AnimalManager.request_wave()` が **15m 以内の動物を みんな** 応えさせ(`wave_back()` で ぴょこっと喜び+ハート)、やさしい「ピョン」音。**近くに 誰も いなくても いちばん近い子(60m 以内)が 必ず 1 匹 応える**(呼びかけが むなしく ならない=子供配慮)。
- **B-1 かくれんぼ動物**(新規 `scripts/world/hide_and_seek.gd`、`main.gd` が起動時に生成): 地上の 5 か所に ちいさな子が 隠れている(`SPOTS`)。近づく(`FIND_RANGE=4.5`)と **ぽよんと大きくなりながら ぴょこんと飛び出す**(やさしい上向きバウンス+にじいろキラキラ+「ピロリン」音)。みつけた数は `game_state.hidden_found`(id 配列・重複なし)に記録し **セーブ**(`save_system` に `"hidden"` 追加=増えるセーブ項目は 1 つだけ)。図鑑の **どうぶつタブに「かくれんぼ: ○/△ みつけた」**、ぜんぶで「**かくれんぼ はかせ!**」(`book.gd` がラベルを動的追加)。過去にみつけた子は 次回 立って待っている。

子供配慮: 飛び出しは「びっくり」でも「怖い」ではない(明るい音・ゆっくり・暗くしない)。失敗概念なし(みつけられなくても損しない・必ず良いこと)。手振りの相手は なかよし前でも応える(あいさつ)。セキュリティ原則(外部通信なし・入力欄なし・PII なし)維持。

検証: 9 スクリプトの構文チェック通過、`Main.tscn` を headless 起動して 実行時エラーなしを確認(`for_mobile=false` 維持)。**改善さんの PC ブラウザ/iPad 実機での 体感確認待ち**(手振りの気持ちよさ・動物の応え方、かくれんぼの 隠れ場所の難度・飛び出しの可愛さ)。

## v0.48.1 — 2026-06-03 — 改善さんフィードバック: ゆきのくにの まぶしさ軽減 / 巡航で丘を すり抜けない

- **ゆきのくにが まぶしい → 軽減**(`yuki_land.gd`): 雪が 真っ白(0.96)で 環境光/太陽も強く、グロー(ブルーム)で 眩しかった。雪を 白すぎない青白(`SNOW`(0.84,0.87,0.94)・`SNOW_DK`(0.72,0.78,0.88))に、`_apply_yuki_env` の ambient 0.75→0.55・sun 0.95→0.78・fog/ambient色も少し落とした。**雪らしい明るさは保ちつつ 眩しさを解消**(オーロラのやさしい光は維持)。
- **自動運転で 丘を すり抜ける → 解消**(`yuki_land.gd` / `dino_land.gd`): 巡航は物理OFFのため、巡航ループ(半径28)に**飾りの丘が食い込んでいた**ので 乗り物が丘を通過していた。丘の配置半径を **34/36 → 52起点**(内縁が34以上)に押し出して 巡航リングの外(背景)へ。ゆきの かまくらも 巡航リングの内側へ寄せた。**submarine は巡航が高所(y9)で 低い砂山の上を飛ぶため 変更不要**(確認済)。

検証: 構文チェック通過、`AUTO_YUKI`/`AUTO_DINO` で **雪が眩しくない・丘が外周の背景に移り 巡航路がすっきり(乗り物が丘を通らない)** ことをスクショで確認。クリスタル/たまご等の他の要素・巡航・帰還は不変。`for_mobile=false`・安全原則維持。

## v0.48.0 — 2026-06-03 — 集めると ごほうび: げんき=スピードアップ / ほし=お祝い+ほしのき / なかよし=プレゼント

これまで ほし/なかよし/げんき は ただのカウンターだった(ミッション以外 何も起きない)。集める動機と「やったね!」を増やすため 3つに ごほうびを付けた(改善さん選択)。どれも `energy`/`star_count` から都度計算=**セーブ項目は増やさない**。

- **げんき → スピードアップ(永続・上限つき)**(`player.gd` + 新 `reward_manager.gd`): `player.gd` の移動を `SPEED * speed_scale * energy_speed_scale` に(地上 `_camera_relative_move` と 月の `_planet_move` の両方=全モード)。`reward_manager` が `energy_speed_scale = min(1.0 + energy*0.03, 1.5)` を計算して設定(げんき1こ+3%・**上限+50%=怖いほど速くしない**)。げんきは既にセーブ→永続。起動時は静かに適用(通知なし)。
- **ほし → お祝い + ほしのき**(新 `reward_manager.gd`、Main 直下 `RewardManager`): ①`star_count` が 5の倍数を越えるたび `show_notice("ほし Nこ オメデトウ!")` + キラキラ + やさしい音 + ほしのきが弾む(**起動時の読込分は祝わない**=`_last_milestone` を読込値で初期化)。②スポーン前方 `(6,-14)` に **ほしのき**(幹+葉+実20個+てっぺん星)。集めた数だけ 実が 金色に光る(全部で てっぺんも光る)=見えるトロフィー。`star_count` から都度再構成。
- **なかよし → プレゼント(ほし)**(`animal_manager.gd`): なかよし済みの近くの動物が **12〜20秒ごとに「ほし」をくれる**(`_try_gift`: 近くの なかよしを1匹選び ぴょこっと喜び + 金キラキラ + `show_notice("○○ が ほしを くれた!")` + `add_star()` + やさしい「ピロン」)。なかよしが多い/近いほど もらえる。→ ほしが増えて ほしのき&お祝いに繋がる気持ちよいループ。
- `Main.tscn` load_steps 64・`RewardManager` 登録。`game_state`/`save_system`/ミッション/既存ワープは不変。

検証: 全スクリプト構文チェック通過、Main 起動エラーなし。`AUTO_REWARD` で **①`add_energy(10)`→speed 1.30 / `add_energy(110)`→1.50(上限) ②`add_star`×7→star_count 越えで お祝い・ほしのきの実が点灯(スクショで金の実+「オメデトウ!」確認) ③なかよし済みの動物の `_try_gift`→ star +1** を確認。検証で膨らんだローカル `user://save.json` はリセット(リポジトリ非対象・配信版に影響なし)。`for_mobile=false`・安全原則維持。
**実機確認待ち(改善さん iPad)**: ①だんだん速くなるのが 気持ちよいか・速すぎないか ②お祝い・ほしのきが 嬉しいか ③動物のプレゼントが 嬉しいか・頻度は適切か ④音量。

## v0.47.0 — 2026-06-03 — わかりやすさ: おでかけメニュー「どこへ いく?」で 6つの世界へ 1タップ

ゲームが大きくなり(13ミッション+6つの別世界、乗り物がマップに散在=潜水艦は遠くて見つけにくい)、3〜7歳に「どこで何を」が分かりにくい問題への対応。改善さんの選択は **おでかけメニュー**。乗り物は残しつつ、迷ったときの やさしい近道を足す。

- **新規 `scenes/ui/WorldSelect.tscn` + `scripts/ui/world_select.gd`**(Main 直下 `WorldSelect`): 図鑑(`book.gd`)と同じ overlay 方式(CanvasLayer layer=2・`process_mode=Always`・暗幕+中央パネル・開くと `get_tree().paused=true`)。タイトル「どこへ いく?」+ **6つの絵カード(3×2)**(つき/そら/うみ/おかし/きょうりゅう/ゆき)。各カード= 世界の色のスウォッチ + 大きいひらがな名 + 乗り物名 + 透明ボタン、白い角丸+世界色のフチの かわいい額縁。**タップで すぐ その世界へワープ**(`close()`→その世界の `warp_in()`)。
- **HUD「どこへ いく?」ボタン**(`TouchHUD.tscn` `WorldButton`、桃色): 上部左(おとな の右)。**地上にいて 電車に乗っていない時だけ表示**(世界の中・乗車中は隠す=二重ワープ防止)。表示制御と押下=open は `world_select.gd` が直接管理(各世界が自分のワールドボタンを持つのと同じ流儀)。`touch_hud.gd` はバウンス配線のみ。
- **各世界6つに 公開 `warp_in()` + `is_active()`**(`moon_trip`/`sky_castle`/`submarine`/`candy_land`/`dino_land`/`yuki_land`): `warp_in` は `if _busy or 滞在中: return` の後に **既存の出発処理**(`_go_moon`/`_go_sky`/`_dive`/`_go_candy`/`_depart`)を呼ぶ薄いラッパ。乗り物はプレイヤーへ reparent されるので**現在地に関係なくワープ**できる。新規ワープロジックは増やさない。
- `Main.tscn` load_steps 62→63・`WorldSelect` 登録。ミッション文言・乗り物・既存の近接ワープは そのまま(メニューで到達しやすくなるだけ)。

検証: 全スクリプト構文チェック通過、Main 起動エラーなし。`AUTO_WORLDSEL` で **①地上で「どこへ いく?」表示=true ②メニュー6カード描画(色+ひらがな) ③`warp_in`→on_moon=true(どこからでもワープOK) ④世界の中ではボタン非表示=false** を確認(`screenshot_worldsel.png`)。`paused` は close で必ず解除。`for_mobile=false`・安全原則(外部通信/入力欄なし)維持。
**実機確認待ち(改善さん iPad)**: ①「どこへ いく?」が押しやすい大きさ・位置か ②カードが 絵として分かるか ③1タップで行けて嬉しいか ④戻れて安心か ⑤メニューを開いている間 世界が止まって違和感ないか。

## v0.46.1 — 2026-06-03 — 改善さんフィードバック: 帰るボタンを上へ / 乗り物と主人公の向きを揃える

- **ワールドのボタンを 画面上部へ**(`TouchHUD.tscn`): 「おうちへ かえる」等が 画面中央(offset_top 278)で 正面を ふさいでいた → **全6つの ワールドボタン(Moon/Airplane/Sub/Candy/Dino/Yuki)を 上部(offset_top 22)に統一**。これらは同時に表示されない(乗り物の駐車場所が離れている/別世界は1つずつ)ので 同じ位置でも重ならない。中央の視界が広く使える。
- **巡航中の 向きを 揃える**(`dino_land.gd` / `yuki_land.gd` `_drive_cruise`): これまで 乗り物だけ 進行方向へ回し プレイヤー本体は回さなかったため、**子の主人公(キャラ)が 乗り物と 別の方を向いて見えた**。**プレイヤー本体ごと 進行方向へ向ける**ように変更(子の 乗り物と主人公が そろって前を向く)。カメラは プレイヤーの位置だけ追従し 向きには影響されない設計なので 酔わない。帰還時に `_player.rotation` を 0 に戻す。(submarine は 主人公が艇内で見えにくいため 据え置き)
- `.uid` 生成: `candy_land.gd.uid` / `dino_land.gd.uid` / `yuki_land.gd.uid`(エディタ/エクスポートで自動生成・追跡)。

検証: 構文チェック通過、`AUTO_YUKI` で **「おうちへ かえる」が 上部に出て 中央の視界を ふさがない**ことを確認、巡航は引き続き正常(moved≈10.8)。`for_mobile=false`・安全原則維持。

## v0.46.0 — 2026-06-03 — 第8の目玉: そりで「ゆきの くに」を 自動運転で めぐる

改善さんの「次のワールドを」→ AskUserQuestion で **ゆきの くに(おすすめ)** を選択。潜水艦/恐竜と同じ自動巡航骨格。明るい雪あかり・怖くない。

- **新規 `scripts/world/yuki_land.gd`**(Main 直下 `YukiLand`):
  - **地上の そり**: 草原(`SLED_DOCK=(-56,8)`、他の乗り物と離す)に かわいい赤いそり。近づくと HUD `YukiButton`「ゆきの くにへ いこう」。
  - **自動巡航**: 押すと フェード → 遠く `YUKI_POS=(0,60,-2600)`。そりをプレイヤーに装着し物理OFF、`_drive_cruise` が ループを一定速度で周回(しゅーっ・酔わない・カメラ追従)。
  - **ゆきの くに**: 雪原・雪の丘・雪をかぶった もみの木・**ゆきだるま**(にんじん鼻・バケツ帽子)・**ペンギン**(ぴょこぴょこ)・**しろい うさぎ**・**かまくら**・**あったか温泉**(湯けむり)・**オーロラ**(やさしく光る帯・ゆっくり明滅)・**ふわふわ降る雪**(GPUParticles)。明るい夕暮れ空(暗い吹雪にしない=怖くない)。
  - **ごほうび クリスタル**: 巡航路の 光る氷の クリスタルに近づくと自動獲得 → ほし+1 ＋「クリスタル ゲット!」。
  - **帰る**: 「おうちへ かえる」常時表示。やさしい きらきら音。env は海/おかし/恐竜と同じく fog 退避/復元。
- **記録/ミッション/HUD**: `game_state.visited_yuki`+`set_yuki_visited()`+セーブ、ミッション「そりで ゆきの くにへ いこう」、`TouchHUD.tscn` に `YukiButton`(全ワールドボタンと同じ 278 スロット=同時に出ない)。`Main.tscn` load_steps 61→62。`auto_capture.gd` に検証用 `AUTO_YUKI`。

検証: 全スクリプト構文チェック通過、Main 起動エラーなし。`AUTO_YUKI` で **到着(on_yuki=true・巡航高さ player_y=61.6=落ちない)→ 自動巡航が3秒で約10.7m前進 → クリスタル獲得 → 帰還** を確認(複数回再実行で安定)、スクショで **オーロラ・ペンギン・うさぎ・ゆきだるま・かまくら・もみの木・そり** と「おうちへ かえる」ボタンが カメラと重ならず読めることを確認(`screenshot_yuki/yuki_ride/yuki_close.png`)。`for_mobile=false`・安全原則維持。
**実機確認待ち(改善さん iPad)**: ①雪の世界が嬉しいか・怖くないか(明るさ・オーロラ)②自動巡航が酔わないか ③ペンギン/うさぎ/ゆきだるまが かわいいか ④クリスタルが分かるか ⑤fps(雪の粒・オブジェクト数)⑥地上の そりの場所(-56,8)が他と重なっていないか。

## v0.45.0 — 2026-06-03 — 第7の目玉: サファリカーで「きょうりゅうランド」を 自動運転で めぐる

改善さんの「きょうりゅうの国を」のリクエスト。AskUserQuestion で示した **やさしい きょうりゅうランド(サファリカーで自動めぐり)** を実装。潜水艦と同じ自動巡航骨格。**草食のやさしい恐竜だけ**(ティラノ/牙/襲う系なし・大きな声を出さない)。

- **新規 `scripts/world/dino_land.gd`**(Main 直下 `DinoLand`):
  - **地上の サファリカー**: 草原(`SAFARI_DOCK=(8,-58)`、他の乗り物と離す)に サファリ色のジープ(ロールバー・しま模様・大きい車輪・ヘッドライト)。近づくと HUD `DinoButton`「きょうりゅうランドへ いこう」。
  - **自動巡航**(潜水艦と同方式): 押すと フェード → 遠く `DINO_POS=(2600,60,0)` の きょうりゅうランドへ。サファリカーをプレイヤーに装着し `set_physics_process(false)`+gravity0、`_drive_cruise` が ループ waypoint を一定速度で周回(カーは進行方向へ yaw、車輪も回る、既存カメラ追従)。操作いらず・酔わない。
  - **やさしい きょうりゅう**: 首の長い草食竜(サウロポッド・首が もぐもぐ揺れる/1体は ゆっくり歩く)・トリケラトプス(やわらかいツノ)・ステゴサウルス(背中の パステルプレート)・**あかちゃん恐竜**(まるい大きな目・ぴょこぴょこ)・空を ゆっくりとぶ プテラノドン。ジャングル(木・しだ・岩)・明るい昼の空。
  - **ごほうび たまご**: 巣の たまごに近づくと **かえって あかちゃんが でてくる** → ほし+1 ＋「たまご かえった!」+ キラキラ。やさしい鳴き声(低め・小音量)。
  - **帰る**: くにに いる間「おうちへ かえる」を常時表示。env は海/おかしと同じく fog を退避し帰還時に復元。
- **記録/ミッション/HUD**: `game_state.visited_dino`+`set_dino_visited()`+セーブ、ミッション「サファリカーで きょうりゅうランドへ いこう」、`TouchHUD.tscn` に `DinoButton`(潜水艦/おかしと同じ 278 スロット=全ワールドボタンは同時に出ない)。`Main.tscn` load_steps 60→61。`auto_capture.gd` に検証用 `AUTO_DINO`。

検証: 全スクリプト構文チェック通過、Main 起動エラーなし。`AUTO_DINO` で **到着(on_dino=true・巡航高さ player_y=62.4=落ちない)→ 自動巡航が3秒で10.2m前進 → 帰還(地上復帰)** を確認、スクショで **首長竜・トリケラ・ステゴ・あかちゃん・サファリカー・ジャングル・たまご** と「おうちへ かえる」ボタンが カメラと重ならず読めることを確認(`screenshot_dino/dino_ride/dino_close.png`)。`for_mobile=false`・安全原則維持。
**実機確認待ち(改善さん iPad)**: ①恐竜が こわくないか(やさしいか)②自動巡航が酔わないか ③あかちゃん・たまごが かわいい/嬉しいか ④fps(恐竜の数)⑤地上の サファリカーの場所(8,-58)が他と重なっていないか。

## v0.44.0 — 2026-06-03 — 第6の目玉: おかしの きしゃで「おかしの くに」を 歩いて探索

改善さんの「新しいワールドを」のリクエストで、AskUserQuestion で **おかしの くに(おすすめ)** を選択。ロケット→月 / 飛行機→空の城 / 潜水艦→海底 に続く別世界。潜水艦のワープ骨格を流用しつつ、**歩いて まわる**甘い世界(自動運転ではない)。

- **新規 `scripts/world/candy_land.gd`**(Main 直下 `CandyLand`):
  - **地上の おかしの きしゃ**: 草原(`CANDY_DOCK=(44,-30)`、ロケット/飛行機/潜水艦と離す)に チョコの機関車(アイシング屋根・ガムドロップ・あめ車輪・わたあめの煙)を駐車。近づくと HUD `CandyButton`「おかしの くにへ いこう」。
  - **おかしの くに**(遠く `CANDY_POS=(2000,60,-2000)`、初回のみ生成): 押すと フェード → テレポート。**歩ける床(`StaticBody`+`CylinderShape` 当たり判定)**+ 見えない壁(リング状 箱コリジョン)+ 落下安全網(床より下で中央へ戻す)で**落ちない=怖くない**。中身: チョコの丘(アイシング)・ロリポップの木・クッキーの家・ゼリーの川(半透明)・カップケーキ・キャンディケーン・**わたあめの くも**・**グミの どうぶつ**(半透明・ぷるぷる弾むくま)。
  - **あまいお菓子(げんき)**: 床に浮く マカロン/ドーナツに近づくと自動獲得 → `add_energy(1)` +「おかし もぐもぐ! げんき アップ!」(既存 げんきシステムと連動)。**ほし**も数個(`add_star`)。近接自動・タッチ不要・公平に良い結果。
  - **帰る**: くにに いる間は「おうちへ かえる」を常時表示(いつでも帰れる)。やさしい きらきら音。
  - 明るいパステルの空(env 退避/復帰。海と同じく fog を退避し帰還時に復元)。`set_petals` で月/空/海と同様に さくら停止(いまは CherryPetals 撤去済みなので no-op ガード)。
- **記録/ミッション/HUD**: `game_state.visited_candy`+`set_candy_visited()`+セーブ、ミッション「おかしの きしゃで おかしの くにへ いこう」、`TouchHUD.tscn` に `CandyButton`(潜水艦ボタンと同じ位置=両者は同時に出ない・カメラボタンと重ならない)。`Main.tscn` load_steps 59→60。`touch_hud.gd` バウンス配線。`auto_capture.gd` に検証用 `AUTO_CANDY`。

検証: 全スクリプト構文チェック通過、Main 起動エラーなし。`AUTO_CANDY` で **おかしの くにへ到着(on_candy=true・床(60)の上 player_y=62=落ちない)** を確認、スクショで **チョコの丘・ロリポップ・クッキーの家・カップケーキ・わたあめ雲・グミのくま・おかし機関車** と「おうちへ かえる」ボタンが カメラと重ならず読めることを確認(`screenshot_candy/candy_ground/candy_player.png`)。`for_mobile=false`・安全原則維持。
**実機確認待ち(改善さん iPad)**: ①甘い世界が嬉しいか・怖くないか ②歩いて落ちないか(壁・安全網)③お菓子で げんき・ほしが分かるか ④いつでも帰れて安心か ⑤fps(オブジェクト数)⑥地上の きしゃの場所(44,-30)が他と重なっていないか。

## v0.43.0 — 2026-06-03 — 性能の予防策: 列車描画の最適化(マテリアル共有 + 窓 MultiMesh)

過去のプロダクト評価で唯一意図的に残していた積み残し(③ 列車描画の最適化)。iPad mini 30fps の予防策。**見た目は不変**で、マテリアル生成数と draw call を削減する。

- **マテリアル共有(色キーでキャッシュ)**(`train.gd` `_make_material`/`_make_unshaded_material`/`_make_emission_material`): 旧実装は呼ぶたびに新しい `StandardMaterial3D` を生成していた。窓・車輪・台車・連結部・ヘッドライトは色が**定数**(全11編成で同一)なのに、編成×車両×個数ぶん別インスタンスを作り **1000 個超** のマテリアルが生成されていた。色+粗さ+種別をキーにした **static 共有キャッシュ** に変更(全編成・全シーンで 1 つ)。定数色のパーツは全部 1 個に集約され、マテリアル数は十数個程度に激減 → VRAM とレンダラーの状態切替コストを大幅削減。
- **窓を車両ごとに MultiMesh 化**(`train.gd` `_attach_windows`): 窓は編成全体で**数百個**(5両×5〜6個×2面×11編成 ≈ 600+)あり、同一メッシュ・同一マテリアル・静止。これまで 1 個ずつ MeshInstance3D = そのまま draw call だったのを、**車両ごとに 1 つの MultiMesh(=1 draw call)** に集約。窓だけで draw call を約 600 → 約 55 に削減。
- **車輪は据え置き**(意図的): 走行で回転する(`_spin_wheels`)ため MultiMesh 化は per-frame の transform 書き換えが要りリスクがある。すでに近接ゲート(110m 以内のみ回転)済み。iPad で なお重ければ次の候補(台車・車輪の MultiMesh 化)。

検証: 全スクリプト構文チェック通過、Main.tscn 起動でスクリプトエラーなし(マテリアルキャッシュ・窓 MultiMesh 生成 OK)。AutoCapture の `AUTO_RIDE`(乗車屋根上)と `TRAIN_CLOSE` で **車体・アクセント帯・パンタグラフ・窓が従来どおり正しく描画**(見た目の変化なし)を確認。`for_mobile=false`・安全原則維持。
**実機確認待ち(改善さん iPad)**: ①列車まわりの fps が軽くなったか(特に駅・複数編成が映る場面)②見た目が以前と変わっていないか(窓・色)。

## v0.42.0 — 2026-06-03 — 改善さんフィードバック: さくら削除 / 草をリアル&まんべんなく / カメラ左右反転

改善さんの「さくらふぶきが全体にマッチしない→削除」「地面の葉っぱをもっとリアルに、まんべんなく」「カメラの左右ボタンで観れる範囲を反対に」の3点。

- **さくらふぶきを削除**(`scenes/Main.tscn`): Player 子の `CherryPetals`(GPUParticles)ノードと ext_resource を撤去(load_steps 60→59)。月/空/海の各スクリプトの `_set_petals` は `if _petals:` ガードで無害化(ノードが無ければ何もしない)。`CherryPetals.tscn` 自体は将来用に残置(シーンからの参照のみ削除)。
- **草をリアルに&まんべんなく**(`assets/shaders/grass.gdshader` + `scripts/world/grass.gd`):
  - **X字クロスメッシュ化**: これまで 1 枚ペラのクアッド(横から見ると消えて薄っぺら)→ 2 枚直交の X 字(`SurfaceTool` で ArrayMesh、`cull_disabled`)。どの向きからも草が見えて立体的に。MultiMesh のまま **1 draw call** を維持。
  - **まんべんなく**: `FIELD_RADIUS` 95→**170**(中央だけでなく広く)、`BLADE_COUNT` 1400→**4200**(同程度の密度を保って広域に散らす)。草原(高さ -1〜9m・湖外)だけに生やす条件は維持。
  - **色をリアルに**: シェーダーで 根元=濃い緑 / 先端=明るい緑 のグラデーション + **株ごとの濃淡ゆらぎ**(位置ハッシュ `color_variation`)で まだらに。風も 2 方向に揺らして自然に。
- **カメラ左右ボタンを反転**(`scripts/world/camera_rig.gd` `rotate_view`): `_yaw_target += dir*STEP` → `-=` に。右ボタンで左側、左ボタンで右側が見えるよう、観れる範囲を反対にした(`touch_hud.gd` の配線はそのまま)。

検証: 全スクリプト構文チェック通過、Main.tscn 起動でスクリプトエラーなし(CherryPetals 撤去・草クロスメッシュ生成 OK)。AutoCapture(SINGLE/PLAYER)で **草が画面全体にまんべんなく広がり立体的・さくらの花びらが出ない** ことを確認。`for_mobile=false`・安全原則維持。
**実機確認待ち(改善さん iPad)**: ①さくらが無くなって世界観が合うか ②草がリアルでまんべんなくか・**fps への影響(株数4200・範囲170m。重ければ `BLADE_COUNT`/`FIELD_RADIUS` を下げる)** ③カメラ左右ボタンの向きが直感に合うか。

## v0.41.0 — 2026-06-03 — 第5の目玉: 潜水艦で「うみの そこ」を自動運転で探索

改善さんの「湖で海底へ。潜水艦で 海の中を 自動運転で探索。魚がたくさん」を実装。ロケット→月、飛行機→空の城 に続く別世界。`sky_castle.gd` のワープ骨格を流用。

- **新規 `scripts/world/submarine.gd`**(Main 直下 `Submarine`):
  - **湖に水面**: これまで湖は くぼみだけで水が無かった → `TerrainHeight.compute_water_y()` の高さに 半透明の水色ディスク(当たり判定なし)を追加。**かわいい黄色い潜水艦**を浮かべて駐艇。近づくと `SubButton`「うみに もぐる」。
  - **潜航/自動巡航**: 押すと フェード → 海の世界へ。潜水艦をプレイヤーに装着し `set_physics_process(false)`+gravity0。**サンゴの庭を囲む ゆるい閉ループを連続自動巡航**(`_process` で waypoint を一定速度で進む・潜水艦は進行方向へ・既存カメラ追従)。操作いらず・酔わない。
  - **海の世界**(遠く深所 `SEA_POS`、初回のみ生成): **明るい青の水中**(暗くしない=怖くない)・砂の海底・**色とりどりのサンゴの庭**(枝/扇/丸)・ゆれる海藻・岩・宝箱・**たくさんの魚**(6群れ計48匹が泳ぐ + 大きな やさしい くじら・かめ。サメ無し)・海底からの**泡**。
  - **ごほうび**: 巡航路の **しんじゅ(光る玉)** を 通り過ぎると自動獲得(タッチ不要)→ ほし+1 ＋「しんじゅ ゲット!」。
  - **もどる**: 海では「うみから あがる」を常時表示 → いつでも浮上(「ぷはー!」)。やさしいソナー音。
- **記録/ミッション/HUD**: `game_state.visited_submarine` + セーブ、ミッション「せんすいかんで うみの そこへ いこう」、`TouchHUD.tscn` に `SubButton`(Moon/Airplane と重ならない位置)。`Main.tscn` load_steps 59→60。
- **env 退避/復帰の堅牢化**: 海で `fog_density/fog_enabled` を変えるため、起動時に地球の fog 設定を退避し、浮上時に復元(DayNightCycle 再適用で戻らない分を明示復元)。月/空と同じく background/ambient/fog色/太陽/さくらは対で退避・復帰。

検証: 全スクリプト構文チェック通過、Main 起動エラーなし。`AUTO_SEA` で **潜航(深度 -590.6=海中)→ 自動巡航が3秒で10.6m前進 → しんじゅ自動獲得 → 浮上(地上復帰)** を確認、スクショで **湖の水面+黄色い潜水艦 / 明るい海中に魚たくさん・サンゴ・くじら** を確認(`screenshot_sub_dock/sea/sea_coral/sea_cruise.png`)。`for_mobile=false`・安全原則維持。
**実機確認待ち(改善さん iPad)**: ①潜るのが楽しいか・酔わないか ②水中が怖くない明るさか ③魚がたくさんで嬉しいか ④いつでも浮上できて安心か ⑤音量。

## v0.40.0 — 2026-06-03 — 仕上げ調整: 子供が遊びやすく(導線・夜・空の城)

評価で見つかった「わかりにくい・怖い・存在感が薄い」点を底上げ。子供配慮(怖くない・わかる)と安全原則は維持。

- **導線(何をすればいいか)**(`title.gd` + `mission_manager.gd`): タイトルの「はじめる」で `signal started` を出し、`mission_manager` が**いまのミッションを通知(`show_notice`)でやさしく案内**(例「ミッション: でんしゃに のってみよう」)。ミッション達成時は「ミッション クリア!」の少しあとに**次の目標も通知**。情報パネルは従来どおり「メニュー」で開く方式を維持(改善さんの以前の決定を尊重・情報過多を避ける)。
- **やさしい夜(怖くない)**(`sky_color.gd`): 夜が暗すぎたのを改善。`NIGHT_AMBIENT_ENERGY` 0.10→0.17、`NIGHT_BG` を少し明るい青紫(0.22,0.26,0.46)、`NIGHT_SUN_ENERGY` 0.3→0.42(月あかり感)。プレイヤー・電車・草原・星がはっきり見える「やさしい夜」に(昼/朝焼け/夕焼けは維持)。
- **空の城を堂々と**(`sky_castle.gd`):
  - **城を大きく**: 天守 7×8×7→**10×12×10**、二層のそり屋根・大きい青屋根/金飾り/窓、四隅の塔 高さ7→**10**・半径と屋根拡大・旗位置up・配置±6→±7.5、門も拡大。島(R24)の主役に。
  - **雲を平らに(城が埋もれない)**: 雲本体が球 scale0.42 で歩く面より約9m上に膨らみ城を半分埋めていた → **上面が歩く面(center.y)に来る薄い雲**に。ふち/底の ぷくぷくに影色(`CLOUD_SH`)を混ぜて立体感(真っ白で平坦に見えない)。
  - **到着導線**: 到着位置を**城を正面に見る位置**へ。到着時に ピンクの歓迎バースト + 少しあと「ほしを あつめよう!」通知。**最寄りに 大きく明るい「めじるし」のほし**を1個置いて目的を分かりやすく。
- **HUDボタンの重なり回避**(`TouchHUD.tscn`): `AirplaneButton` を下げて `MoonButton` と十分離す(offset_top 110→150)。

検証: 全スクリプト構文チェック通過、Main 起動エラーなし(title→mission 配線含む)。`AUTO_SKY` で**城が大きく雲に埋もれず堂々と見える・雲が立体的・到着位置から城が見える・めじるしほし**を確認(`screenshot_sky_castle/arrival/island.png`)。`FOUR_TIMES`(真夜中 0.08)で**夜が怖くない明るさ**を確認(`screenshot_night.png`)。`for_mobile=false`・安全原則維持。
**実機確認待ち(改善さん iPad)**: ①開始時に「何をするか」分かるか ②夜が怖くないか ③空の城の城が堂々と見えるか・ほしを探せるか ④雲が分かりやすいか ⑤月/飛行機ボタンが重ならないか。

## v0.39.0 — 2026-06-03 — 改善さんフィードバック反映: 月を惑星化 / さくら停止 / 空の視認性 / 眼

- **月を「小さな惑星」に(裏側まで歩ける)**(`player.gd` + `camera_rig.gd` + `moon_trip.gd`): 旧ドーム(ふちに見えない壁)をやめ、**重力を球の中心へ向ける惑星モード**を追加。`player.gd` に `planet_mode`/`planet_center` を足し、`_planet_process`(球中心へ重力・`up_direction`=球面法線・接平面へ射影した移動・ジャンプは法線方向・体を法線に沿って傾ける)を実装(地上/空は従来の Y 重力のまま=`planet_mode=false`)。`camera_rig.gd` に `set_surface_up()` を追加し、惑星では「上」を球面法線に合わせる(裏側でも頭が上=酔いにくい。`UP` のときは従来と完全一致で地上/空に回帰なし)。月本体は半径22mの1つの球+`SphereShape3D`、ふちの壁は撤去(球なので落ちない)。装飾(ロケット=てっぺん、もちつき/もちだんご/月面カー/クレーター)は **球面の法線に合わせて配置**(`_place_on_planet`/`_surface_basis`)。もちつきは傾いた root の下に local で再構成。
- **さくらふぶきを 月・空で停止**(`moon_trip.gd` + `sky_castle.gd`): `CherryPetals`(Player の子で常時追従)を、月/空に着いたら `emitting=false`、地球に戻ったら `emitting=true`(宇宙・空の上に桜は不要)。
- **空の城の視認性を改善**(`sky_castle.gd` `_apply_sky_env`): `ambient_light_energy` 0.95→0.5(白飛び解消・陰影で立体感)、背景を すこし濃いめの空色(`SKY_BG` を `(0.45,0.68,0.95)`)にして 雲・城・電車が はっきり見えるよう コントラスト確保。
- **主人公の眼の出っ張りを解消**(`player.gd` `_build_head`): 顔は球なので端では表面 z が手前(-0.42)まで来ないのに 目/キラキラを -0.42〜-0.47 に置いて突出していた。各パーツを「その(x,y)での顔表面」に沿わせる `_face_pt()` で配置し、奥行きスケールも薄く(0.32)して 平らな目のように顔に貼り付け、出っ張らないように。ほっぺも同様。

検証: 全スクリプト構文チェック通過、Main.tscn 起動エラーなし。`AUTO_MOON` の**惑星歩行テスト**で前進し続けて **中心からの距離が R=22 で一定(球面に居続ける=落ちない/飛ばない)**・`updot` が 1.00→0.08(てっぺんから赤道付近まで連続歩行)・カメラ上向きが法線追従(`screenshot_moon_walk.png` で頭が上・地平線水平)を確認、月面カーの reparter も健在。`CHAR` ビューで眼が顔に沿って出っ張らないこと、`AUTO_SKY` で空が濃い青+コントラスト改善・桜が降らないことを確認。`for_mobile=false`・安全原則維持。
**実機確認待ち(改善さん iPad)**: ①月で球面を歩いて酔わないか・楽しいか ②空が見やすくなったか ③眼の見た目 ④月/空で桜が消えるのが自然か。

## v0.38.0 — 2026-06-03 — 第4ウェーブ: 月の充実 + 飛行機で「そらの おしろ」

改善さんの「月をもっと面白く」「飛行機を用意して空の城を作成」を実装。ロケット→月の「別世界ワープ」骨格を流用し、新規物理なし。

### 月をもっと面白く(`moon_trip.gd`)
- **もちだんご(げんき+1)**: もちつきの まわりに 三色だんご(ピンク・白・みどり)を浮かべ、近づくと獲得(タッチ不要・`stars.gd` 方式)→ `add_energy(1)` + 「もちだんご ゲット!」+ キラキラ + やさしい音。お餅つきが遊びにつながる。
- **宇宙の演出**: 大きな**地球がゆっくり回る**(地球+大陸を1ノードにまとめ `_process` で回転)、かわいい**UFO がふわふわ**(上下ループ+ゆっくり旋回)、宇宙のキラキラ。
- **月面カー(のって走る)**: かわいいバギー(オレンジ車体+4輪+水色キャノピー+アンテナ)を駐車。MoonButton を文脈で出し分け(乗車中→「カーから おりる」/近い→「カーに のる」/ロケット近い→「おうちへ かえる」)。乗ると バギーをプレイヤーの子にして一緒に動き、`speed_scale=1.8` で速く走れる(`player.gd` に `speed_scale` 追加)+ やわらかいエンジン音(ループ)。低重力ドーム+ふちの壁で落ちない。

### 飛行機で「そらの おしろ」(新規 `sky_castle.gd` + `AirplaneButton`)
- **地上の飛行機**: かわいいプロペラ機を原点近く(ロケットと離す)に駐機。近づくと「そらへ いく」。乗車中ガードあり。
- **自動遊覧飛行**(改善さんの選択): 乗ると `player.set_physics_process(false)` + 飛行機をプレイヤーに装着し、上昇アークを Tween で飛ぶ → **既存 CameraRig が追従して飛ぶ絵になる**(追加カメラコードなし)→ フェードして空の城へ。操作いらず・酔わない・落ちない。プロペラ回転・プロペラ/風音。
- **空の城ワールド**(遠く・高所 SKY_POS、初回のみ生成): **雲の島**(平らな当たり判定で歩ける+ふわふわ見た目)、**お城**(クリーム壁・青屋根・四隅の塔・金飾り・光る窓・桜色の旗)、**飛び石の雲**(ふわふわ重力 0.4 で渡れる)、**レインボーの橋**、城の上空を**新幹線が旋回**(前回「城を電車が走る」の空版)、**ほし**(近づくと ほし+1)。**夜にしない明るい夢空**(env を退避/復帰)。**落下防止**=ふちの見えない壁+落ちたら島へ戻す安全網(空から落ちる恐怖を作らない)。
- **帰り**: 空の城の飛行機に近づくと「おうちへ かえる」→ 降下飛行→地上へ。飛行機は1機で、着陸した所に駐機(プレイヤーと一緒に移動)。

### 記録・ミッション・HUD
- `game_state.gd`: `visited_sky_castle` + `set_sky_castle_visited()`。`save_system.gd`: `"sky_castle"` を永続化(bool・slug/数値のみ=安全原則順守)。
- `mission_manager.gd`: 「ひこうきで そらの おしろへ いこう」+「ほしを 9こ あつめよう」を追加。
- `TouchHUD.tscn`/`touch_hud.gd`: `AirplaneButton` 追加(押下・表示は sky_castle.gd が直接管理、HUD はバウンス演出のみ)。
- `Main.tscn`: `SkyCastle` ノード登録(load_steps 58→59)。

検証: 全スクリプト構文チェック通過、Main.tscn 起動でスクリプトエラーなし、`AUTO_MOON`(もちだんご・UFO・回る地球・月面カーの のる/おりる reparent: mounted speed=1.8→dismount speed=1.0)と `AUTO_SKY`(地上機→飛行→空の城: on_sky=true・player_y=400.9 で島に着地し落ちない→帰宅 on_sky=false)を確認。`for_mobile=false`・安全原則(外部通信/入力欄/個人情報なし)維持。
**実機確認待ち(改善さん iPad)**: ①飛行が酔わず気持ちよいか ②空の城で落ちて怖くないか ③月面カーが楽しいか ④もちだんご・ほしが分かるか ⑤音量が適切か ⑥Web export 後に「そらへ いく」ボタンが出るか。

## v0.37.0 — 2026-06-03 — 月の作り直し: 大きな球体 + もちつきうさぎ + 帰りのロケット

改善さんのフィードバック反映(`moon_trip.gd`)。月が「落下して怖い」「うさぎが餅つきしていない」「さくら不要」「戻るロケットが欲しい」の4点。

- **月を大きな球体に**(怖さ解消): これまでの灰色の薄い円盤(ふちで宇宙へ落ちる感じ)をやめ、**半径120mの大きな球体**に。上はゆるやかなドームで歩け、ふちの内側はほぼ平ら=落ちる感覚がなく怖くない。`SphereShape3D` の当たり判定で球面に立てる。クレーター・ふちの見えない壁(落下防止)・低い見える縁は球面の高さに合わせて配置。
- **もちつきする うさぎ**(`_build_mochi_pounding`): うす + しろい おもち + **きねを ふりおろす うさぎ(つき手)** + **返し手のうさぎ**(ぴょこぴょこ)。きねは `Tween` ループで ゆっくり上下(やさしい うごき)。静止していた2匹を もちつきの場面に置き換え。
- **さくら(ピンクの旗)を削除**: 月の桜色の旗を撤去。
- **帰りのロケット**(`_build_rocket` を地球/月で共用): 月にもロケットを建て、**近づくと「おうちへ かえる」ボタンが出る**(以前は常時表示)。ロケットで帰る体験に。発射台+ロケットの生成を共通関数に切り出し。

検証: 構文チェック通過、`AUTO_MOON` キャプチャで①球体ドームに立てて落ちない②もちつき2匹+うす+きねが描画③帰りロケット近接で「おうちへ かえる」表示 を確認(`screenshot_moon.png` / `screenshot_moon_mochi.png`)。Main.tscn 起動でスクリプトエラーなし。`for_mobile=false`・安全原則(外部通信/入力欄/個人情報なし)維持。
**実機確認待ち(改善さん iPad)**: ①月の上が怖くないか(落ちる感じがないか) ②もちつきが「餅つき」に見えるか ③帰りロケットが分かりやすいか ④ふわふわ重力が気持ちよいか。

## v0.36.0 — 2026-06-02 — P2 仕上げ: 親モード(設定) + 技術的負債の除去

評価の P2(仕上げ・衛生・追加機能)。列車描画の最適化(別途)を除く分。

- **親モード(おとなのかた / 設定)**(新規 `scenes/ui/Settings.tscn` + `scripts/ui/settings.gd` + HUD「おとな」ボタン): 子供が誤操作しないよう **数字ゲート(1→2→3 の順)** を通った先に設定画面。**おと の オン/オフ**(`AudioServer` マスターのミュート)、**あそんだ かいすう** の表示、**データを けす**(「ほんとうに けす?」の2段階確認つき → セーブ削除してシーン再読込で最初から)。ROADMAP Phase 5-7 の「親モード」。
- **あそんだ かいすう**(`game_state.gd` + `save_system.gd`): 起動ごとに +1 して保存(リピートの記録・親モードで表示)。
- **技術的負債の除去**: `train.gd` の毎フレーム/初期化時の `[DBG Train]` print を削除(Web の console.log 負荷も低減)。公開URLに残っていた `export/web/dbg.html` を削除。`train_data.gd` の陳腐化コメント(「9編成」「楕円トラック」)を現状(11編成・弧長ベース)に修正。

検証: 全スクリプト構文チェック通過、Main.tscn 起動でエラー/警告/DBGログなし、AutoCapture(`AUTO_SETTINGS`)で数字ゲート→設定画面を確認。`for_mobile=false` 維持。安全原則(外部通信/入力欄/個人情報なし)も維持。
**実機確認待ち(改善さん iPad)**: ①「おとな」→数字ゲート→設定 が分かりやすいか ②音オン/オフが効くか ③データけすの確認が安全か ④ゲートが子供に開けられすぎないか。

## v0.35.1 — 2026-06-02 — P1 性能(列車描画以外): 月の遅延生成 / 駅の柱コリジョン削除 / プロンプト間引き

評価の性能指摘のうち、列車の描画最適化(別途)を除く軽量な3点。

- **月コンテンツの遅延生成**(`moon_trip.gd`): 星40・縁36・地球・うさぎ等を起動時ではなく**初回ワープ時に1度だけ**組み立てる。地球プレイ中の常駐ノード/メモリ/カリングコストを削減(発射台は近接判定に要るので最初から生成)。
- **駅の柱コリジョン削除**(`station.gd`): 屋根の柱4本×6駅=24個の StaticBody を撤去(細くてぶつかる必然性が薄い + 物理コスト)。ホーム本体の当たり判定だけ残す。
- **歩行中プロンプト更新の間引き**(`ride_controller.gd`): 「○○に のる?」の最寄り編成探索を毎フレーム→約0.15秒間隔に(11編成走査を 1/9 に)。

検証: 構文チェック通過、AutoCapture(`AUTO_MOON`)で月が初回ワープで正しく生成され表示されること・カメラ基準移動(dot=1.00)を確認。`for_mobile=false` 維持。

## v0.35.0 — 2026-06-02 — P1 コンテンツ強化(図鑑の詳細 + ミッションの目玉ツアー)

プロダクト評価で「最高コスパ」とされた2点。作り込んだ宝物(月・城・運転)を確実に子供へ届け、図鑑のコレクション欲を伸ばす。

- **図鑑(でんしゃ)に詳細パネル**(`train_data.gd` + 全11 `.tres` + `book.gd`): TrainData に `description`(やさしい説明)と `top_speed_kmh` を追加し、11編成すべてに記入。図鑑のでんしゃタブで発見済みのセルをタップすると、大きな車体色・名前・説明・「さいこうそく ◯◯ キロ」を出す詳細パネルが開く(「もどる」で閉じる)。電車好きの子の最大のリピート装置に。
- **ミッションを「目玉ツアー」に拡張**(`mission_manager.gd` + `game_state.gd` + `save_system.gd` + 配線): 基本4つの後に「『うんてん』ボタンで うんてんしゅに なろう」「おしろの でんしゃに のろう」「ロケットで つきへ いこう」を追加。これで自力では気づきにくい運転・城・月へ確実に誘導。GameState に `visited_moon`/`drove_train` フラグを追加(保存対応)。`moon_trip` が月到着時に、`ride_controller` が運転突入時にフラグを立てる。

検証: 全スクリプト構文チェック通過、Main.tscn 起動でエラー/警告なし、AutoCapture(`AUTO_BOOK`)で図鑑の詳細パネル(車体色・名前・説明・最高速度・もどる)を確認。日本語表示も正常。`for_mobile=false` 維持。
**実機確認待ち(改善さん iPad)**: ①図鑑のタップ→詳細が分かりやすいか ②説明文・速度がうれしいか ③ミッションのツアーで月・城・運転に気づけるか。

## v0.34.0 — 2026-06-02 — P0 バグ修正(評価で判明した新機能の境界バグ5件)

プロダクト評価(4観点の独立監査)で判明した、新機能の組み合わせ/エッジで壊れるバグを修正。

- **B1 乗車中の月ボタン抑止**(`ride_controller.gd` + `moon_trip.gd`): `RideController.is_riding()` を追加。乗車中は MoonButton を出さず、押下も無視。乗車状態のまま月へワープして操作不能になる事故を防止。
- **B2 分岐の二重編成を解消(ルート入れ替え方式)**(`ride_controller.gd take_branch`): 乗り換え時、運転編成を行き先ルートへ載せ替えると同時に、行き先ルートに元からいた編成をこちらが抜けた元ルートへ載せ替える(スワップ)。「1ルート1編成」を常に維持し、線路がカラ/編成が重なるのを防ぐ。両 reparent はフェード中点で実行(瞬間移動は見えない)。検証: 11編成・占有ルート11・重複なし。
- **B3 slug の実トラック追従**(`train.gd`): `_active_slug` を追加し `switch_route(new_slug, …)` で更新、`get_route_slug()` を新設。分岐検出を「実際に走っているルート」で照合するようにし、載り替え後に別ルートの分岐点を見て不発になる不具合を解消。
- **B4 月のふち壁を高く**(`moon_trip.gd`): 低重力ジャンプ(到達~13.5m)で飛び越えられないよう、見えない壁を 4m→18m に。
- **B5 駅ホームを歩いて乗れる高さに**(`station.gd`): プラットフォーム段差を 0.5m→0.16m(カプセル半径0.35より十分低く)。検証: 歩いてホームに乗れることを確認(ジャンプ不要)。

検証: 全スクリプト構文チェック通過、Main.tscn 起動でエラー/警告なし、AutoCapture 新 `AUTO_FIXCHECK` で B2(スワップ不変条件)と B5(ホーム登坂)を実測確認。`for_mobile=false` 維持。

## v0.33.0 — 2026-06-02 — HUD情報を「メニュー」ボタンに格納(押したら表示)

改善さんから「HUDメニューは図鑑の右上に同じ大きさにまとめ、押したらメニューが表示されるように」と要望。常時表示だった情報パネルを、ボタンで開閉する方式にして画面をスッキリさせた。

- **「メニュー」ボタン**(`TouchHUD.tscn` + `touch_hud.gd`): 右上の「ずかん」の隣に、**同じ大きさ・同じ青**の「メニュー」ボタンを追加。
- **情報パネルを開閉式に**: ほし/なかよし/げんき/ミッションのパネル(`InfoMenu`)を既定で非表示にし、右上(ボタンの下にドロップ)へ移動。「メニュー」を押すたびに表示/非表示をトグル(`_on_menu_pressed`)。普段は画面が広く使え、見たいときだけ出せる。
- カウンタ・ミッションの更新ロジックは従来どおり(非表示でも内部更新)。

検証: 構文チェック通過、AutoCapture 新 `AUTO_MENU` で①閉(メニュー/ずかんボタンのみ・パネル非表示)②「メニュー」押下で開(ほし/なかよし/げんき/ミッションが右上にドロップ)を確認。`for_mobile=false` 維持。
**実機確認待ち(改善さん iPad)**: ①「メニュー」ボタンが押しやすい大きさ・位置か ②開いたパネルが読みやすいか ③普段隠れていて困らないか。

## v0.32.0 — 2026-06-02 — 柱の撤去 + カメラ基準の移動 + 月へ旅行

改善さんから3点の要望。

- **線路の柱(橋脚)を撤去**(`railway.gd`): 「柱が視界をさえぎって見えにくい」ため、`_build_route` の `_build_piers_for()` 呼び出しを停止。高架・水上の線路は支柱なしで宙に浮く「空にうかぶ せんろ」表現に(お城まわりが特にスッキリ)。`_build_piers_for` 自体は将来用に残置。
- **移動ボタンをカメラ基準に**(`player.gd`): これまで移動はワールド固定で、カメラを回すと「うえ」を押しても画面奥へ進まなかった。`_camera_relative_move()` を新設し、いま映しているカメラの前(画面奥)/右を基準に動くように。**どのカメラ向きでも「うえ」=画面の奥**、キャラは進む向きを向く。旧 `_compute_horizontal_velocity`/`_compute_yaw` は廃止。検証で「カメラを90°回して『うえ』→カメラ前方へ dot=1.00」を確認。
- **月へ旅行**(新規 `scripts/world/moon_trip.gd` + Main 直下 `MoonTrip` + HUD `MoonButton` + `player.gravity_scale` + `camera_rig.snap_to_target()`): 地球の発射台のロケットに近づくと「つきへ いく」ボタン。押すとフェードして月へワープ。月は宇宙の黒い空・星・大きな地球・クレーター・桜色の旗・つきのうさぎ2匹。**重力が弱く(0.16)ふわっと高く跳べる**。「おうちへ かえる」でいつでも帰れる(失敗概念なし)。怖くない配慮: 真っ暗にしすぎない・急に動かない(フェード)・ふちに壁+落下時の自動復帰で宇宙に消えない。遠距離ワープのカメラ長パンは `snap_to_target()` で防止。環境切替は `DayNightCycle` を一時停止して手動設定→帰還時に再開。

検証: 全スクリプト構文チェック通過、Main.tscn 起動でエラー/警告なし。AutoCapture 新 `AUTO_MOON` で①カメラ基準移動(dot=1.00)②城に柱が無いこと③ロケット+「つきへ いく」④月面(地球・星・うさぎ・旗・「おうちへ かえる」)を確認。`for_mobile=false` 維持。
**実機確認待ち(改善さん iPad)**: ①柱が無くて見やすいか ②移動ボタンの向きが直感的か(酔わないか)③月旅行が楽しいか・低重力ジャンプが気持ちよいか ④月が怖くないか(暗さ・音)。

## v0.31.0 — 2026-06-02 — HUD を見やすく(ほし/なかよし/げんき/ミッションを左上メニューに集約)

改善さんから「表示が見えにくい」「左上にメニューでまとめて」と要望。これまで上部に横並び(ほし=左、なかよし・げんき=中央〜右に散らばり)+ ミッションは中央下、と散在して読みにくかった。

- **左上の情報メニュー**(`TouchHUD.tscn` の新 `InfoMenu` パネル + `touch_hud.gd` のパス更新): 白い角丸の不透明パネル(`Style_menu_bg`、alpha 0.93)を画面左上に1枚置き、**ほし → なかよし → げんき**を縦に並べ、区切り線の下に**ミッション**を表示。各カウンタは色分け(金/桃/緑)・font 32 で大きく、ミッションは font 22 で1行に収まるよう調整(長い時は自然に折り返し)。
- **ずかん**ボタンは右上に独立配置(従来どおり押せる)。
- 旧 `TopBar`(横並び)と中央下の `Mission` ラベルを廃止し、`@onready` パスを `InfoMenu/...`・`BookButton` に更新。

検証: 構文チェック通過、Main.tscn 起動でノード未解決・エラーなし、AutoCapture でメニューがまとまって読みやすいこと(ほし5/なかよし2/げんき0/ミッション1行)を確認。`for_mobile=false` 維持。
**実機確認待ち(改善さん iPad)**: ①左上メニューが読みやすいか ②文字サイズ・パネルの大きさが適切か ③ミッションが長い時の折り返しが許容範囲か。

## v0.30.0 — 2026-06-02 — お城(中をくぐる電車 + 上空をめぐる電車)

改善さんのリクエストで、かわいいパステルのお城を建て、その**中を電車がくぐり**、**上空も電車がめぐる**ようにした。データ駆動(route_data + TrainData .tres + Main.tscn の Train ノード)に2編成を追加したので、図鑑(でんしゃ)にも自動で増え、おしろでんしゃは乗車もできる。

- **お城**(新規 `scripts/world/castle.gd`、Main 直下 `Castle` ノード): CASTLE_CENTER(150,135) にスクリプト生成。クリーム色の城壁・四隅の青い円錐屋根の塔・中央の天守(青い円錐屋根+金の玉+桜色の旗)・狭間(クレネル)・金の帯・光る窓・石枠のアーチ。中央に X 方向の大きなアーチ通路(トンネル)。
- **おしろでんしゃ(中をくぐる)**(`route_data.gd` "oshiro" + `resources/train_data/oshiro.tres`): お城のアーチを貫く地上ループ。ループの北点(ratio 0.25)= CASTLE_CENTER で接線が X 方向なので、電車がアーチを真っ直ぐ通り抜ける。紫×金のかわいい編成。
- **そらでんしゃ(上空をめぐる)**(`route_data.gd` "sora" +17m + `resources/train_data/sora.tres`): お城の上空を周回する高架ループ。railway が自動で支柱(橋脚)を立て、空の線路になる。水色×桜色の編成。
- **当たり判定**(`castle.gd`): 通路(トンネル)をふさがないよう、左右の城壁(±Z)と四隅の塔だけ固くした。子供も電車もアーチをくぐれる。高い屋根・天守には付けない(手が届かない)。
- 既存システムとの自動連携: 図鑑は `resources/train_data/` を走査するので11編成に増える。乗車システムは Trains の子を全部見るのでおしろでんしゃに乗れる(そらでんしゃは+17mで地上から届かない)。

検証: 全スクリプト構文チェック通過、Main.tscn ヘッドレス起動で `oshiro`(len139.4)・`sora`(len138.3)が初期化・走行、エラー/警告なし。AutoCapture 新 `AUTO_CASTLE` モードで①全景(お城+上空の高架ループ)②アーチ(おしろでんしゃが通過中)③上空(そらでんしゃが高架を走行)を確認。`for_mobile=false` 維持。
**実機確認待ち(改善さん iPad)**: ①お城の見た目がかわいいか ②アーチを電車がくぐるのが楽しいか ③上空の電車・高架の支柱が多すぎないか(密度)④お城の位置(150,135)が他と被っていないか ⑤fps への影響(支柱・お城のメッシュ数)。

## v0.29.0 — 2026-06-02 — 当たり判定: たてもの・駅をすり抜けないように

これまで当たり判定は**地形とプレイヤーだけ**で、建物・駅・電車・小物などすべてをすり抜けていた(改善さん指摘)。改善さんと相談し、**たてもの(家・お店・ビル)と駅(ホーム)**を固くした。電車・動物・星・木・ベンチ等は意図的にすり抜けのまま(動物=なかよし近接、星=近づいて獲得、動く電車=めり込み/押し出しが怖い、小物=子供が引っかからない)。

- **建物に当たり判定**(`town.gd`): 家・お店・ビルの足元に `StaticBody3D + BoxShape3D`(`_add_box_collision`)を追加。家(3.0×2.4×3.0)/お店(3.2×2.2×3.0)/ビル(3.0×高さ×3.0)。既定レイヤー=1・プレイヤーのマスク=1(地形と同じ)で衝突。木は付けない。
- **駅に当たり判定**(`station.gd`): プラットフォームを箱の当たり判定にして横から通り抜けない+ジャンプでホームの上に乗れる。屋根の柱4本も細い角柱の当たり判定。看板(Label3D)・装飾には付けない。
- 旧 `_build_crossing_on` 削除に続き、当たり判定ヘルパは各ファイルに小さく持たせた(`town.gd`/`station.gd` の `_add_box_collision`)。

検証: 全スクリプト構文チェック通過、Main.tscn ヘッドレス起動でエラー・警告なし。AutoCapture 新 `AUTO_COLLISION` モードでプレイヤーを建物へ歩かせ、**6m→1.85m 手前で停止(壁半幅1.5+カプセル0.35=期待値)・blocked=true**、すり抜けないことを数値とスクショで確認。`for_mobile=false` 維持。
**実機確認待ち(改善さん iPad)**: ①建物・駅にぶつかって止まるのが自然か ②狭い所で引っかかってイライラしないか ③駅のホームにジャンプで乗れるのが楽しいか。

## v0.28.0 — 2026-06-02 — 踏切を本格化(遮断機の開閉 + 交互点滅 + やさしい警報音)

これまでの踏切は静的(横断道路・ポール・赤い光1個・固定の黄色バー)だった。本物らしく動くようにした。新規 `scripts/world/crossing.gd`(自己完結の踏切ノード)を作り、`town.gd` は座標(slug+全長比)を渡して生成するだけにした(旧 `_build_crossing_on` と未使用ヘルパ・色定数を削除)。

- **遮断機(しゃだんき)の開閉**(`crossing.gd`): 自ルートの編成が `APPROACH_DIST`(22m)以内に来ると遮断機バーが下りて道をふさぎ、通り過ぎると上がる。バーはポール上の回転軸を `move_toward` でゆっくり動かす(`OPEN_ANGLE` 約77°、急に動かない=怖くない)。バーは**黄色×黒の縞**(6セグメント)+先端の赤い丸。
- **交互点滅する2つの赤色警報灯**(`crossing.gd`): 閉動作中だけ、左右の赤灯が `BLINK_HZ`(1.6Hz)で交互に点滅(emission のオン/オフ)。開いたら消灯。踏切の象徴的な見た目。
- **×印の警標**(`crossing.gd`): 漢字を使わず、白い斜め十字(×)+黄色いひし形の背板を道路向きに設置。
- **やさしい警報音「カンカン」**(`crossing.gd`): 閉動作中**かつプレイヤーが近い(34m以内)とき**だけ小さく鳴らす(全踏切が常時鳴るのを防ぐ)。柔らかい2打のトーン(1175Hz/988Hz)を角を丸めた包絡線でループ生成、音量 -16dB(うるさくない・刺激的でない)。`AudioStreamWAV` の `LOOP_FORWARD`。
- **接近検知**: 各ルート(komachi 2か所 / e235_yamanote 1か所)には専用編成が1本ずつなので、slug 一致でその編成を引き、ワールド距離で開閉を判定(ワープ式分岐は komachi/yamanote を対象にしないため編成は route から離れない)。`train.get_ride_anchor_position()` を再利用。

検証: 全スクリプト構文チェック通過、Main.tscn ヘッドレス起動でエラー・push_warning なし、AutoCapture 新 `AUTO_CROSSING` モードで①閉(こまち編成が踏切上=接近)②開(別の踏切で遮断機が上がっている)を確認。`for_mobile=false` 維持・外部通信を増やさず。
**実機確認待ち(改善さん iPad)**: ①遮断機の上げ下げが分かりやすく怖くないか ②警報灯の点滅・警報音の音量/音色がやさしいか ③踏切が小さすぎないか(ポール・バーの大きさ)。

## v0.27.0 — 2026-06-02 — 作りこみ本格化 第3ウェーブ(うんてんしゅモード)

改善さんが選んだ方向「電車を深く + 駅まわりを楽しく」を「1テーマを深く磨く」方針で、中心の目玉に **うんてんしゅモード** を実装。乗車体験を「眺める」から「操る」へ。**opt-in**(乗ると今までどおり自動走行。乗車中に「うんてん」を押した時だけ運転操作)で既存挙動は不変(非運転時は throttle=1.0 の恒等乗算)。

- **すすむ・とまる**(`train.gd`): 運転スロットル `_driver_throttle`(目標値へ `move_toward` で ease=バネ感、急発進・急停止しない)を追加。RUNNING の前進量に乗算するだけ(`advance = _linear_speed * _slow_factor_at * throttle * delta`)。`enter/exit_driver_mode()`・`set_driver_throttle()`・`is_driver_stopped()` を公開。throttle≈0 で車輪も自然停止。止めっぱなしでも `_physics_process` は回り続け壊れて見えない。駅の自動停車(DWELLING)は運転中も併用するので、一度も止めなくても駅にはちゃんと止まる(世界が破綻しない)。
- **ぶんきで みちを えらぶ(ワープ式)**(`train.gd` + `route_data.gd` + `railway.gd` + `ride_controller.gd`): 物理レール分岐は作らず、運転中の編成を別ルートの Path3D へ載せ替える `switch_route()`(全 PathFollow3D を reparent + 弧長系 `_length/_stops/_linear_speed/_progress` を差し替え + 即 `_apply_progress()`)。必ず `_transition` のフェード中点で実行し reparent の乱れを隠す。分岐点は `route_data.branches()` にキュレート(本線3編成 はやぶさ/かがやき/N700 は center(0,0) 同心・近接・同 elevation なので `to_ratio≒at_ratio` で位置がほぼ飛ばない。停車点・start を避けた ratio)。`ride_controller._check_branch()` が接近を監視し、手前で2択(「○○ に のりかえ」/「このまま まっすぐ」)を出す。選ばなければ直進(強制も失敗もなし)。乗り換え直後の逆戻り即提示は `_branch_cooldown` で抑止。二重編成は衝突判定が無いので通り抜け(子供には「お友だちの電車」)。
- **添え(駅まわり)**(`ride_controller.gd`): 運転中に自分で減速して駅にぴたっと止めると「ぴったり とうちゃく!」のごほうび(上昇音 `_make_tone(660,990)` + 通知 + 星+1。公平に良い結果へ寄せる)。止めなくても通常の到着通知が出るだけ=失敗概念なし。
- **UI**(`TouchHUD.tscn` + `touch_hud.gd`): 乗車中に出る「うんてん」ボタン(運転中は「じどう」に文言トグル)、運転中だけ出る「ゴー」(緑)「とまれ」(淡パステル赤=怖くない)を左下(運転中は使わない D-pad の上)に配置、分岐2択は中央に縦積み(左右の操作列を避ける)。全ボタン 64px 以上・ひらがな/カタカナ・押下バウンス。降車時は運転 UI を必ず畳む。

検証: 全スクリプト構文チェック通過、Main.tscn ヘッドレス起動でエラー・配線警告なし、AutoCapture(新 `AUTO_DRIVE` モード)で①運転モード突入(ゴー/とまれ表示・じどうトグル・D-pad無効化)②分岐2択の中央表示③乗り換えワープ後の前面展望(かがやきの並走本線へ滑らかに継続)を確認。`vercel.json`/`robots.txt`/`template.html` は不変、外部通信を増やさず、`for_mobile=false` 維持。
**実機確認待ち(改善さん iPad)**: ①ゴー/とまれの加減速が気持ちよいか(THROTTLE_EASE)②分岐2択が分かりやすいか・ワープが酔わず一瞬か ③止めっぱなしで壊れて見えないか ④「ぴったり とうちゃく」のごほうびが嬉しいか ⑤分岐点の出る場所(at_ratio)が適切か。

## v0.26.0 — 2026-06-01 — 作りこみ本格化 第2ウェーブ(乗車体験を深める + 生き物感)

第2ウェーブ5点。乗車中の遊びを増やし、世界に生命感を足す。

- **車内からの眺め視点(C3)**(`ride_controller.gd` + `train.gd` + HUD): 乗車中に「ながめ」ボタンで視点を巡回(やね→うんてんせき→まどぎわ)。`RIDE_VIEWS` プリセット配列化し `cycle_ride_view()` を追加。前面展望は先頭車マウント(`train.get_ride_mount_front()` = `_parts[0]` の PathFollow)。切替はフェードで隠す(酔わない)。HUD に乗車中だけ出る「ながめ」ボタン(200×96、48px 以上)。
- **車内アナウンス(C2)**(`ride_controller.gd` + `train.gd`): 駅到着で「○○ えき に とうちゃく!」、発車で「しゅっぱつ しんこう!」+ ピンポーンのチャイム。`train.gd` に `departed` シグナル追加。乗車中の編成の `arrived`/`departed` を購読し、到着位置の最寄り駅名(`stations_path` 経由)を引いてテロップ。到着音は駅メロが鳴るのでチャイムは発車時のみ。
- **動物が歌う(A3)**(`animal.gd` + `animal_manager.gd`): なかよし済みの動物が、近く(18m以内)にいるとときどき「♪」を出してリズムよくバウンス + 短いメロ。`animal.sing()`(歌唱中は `_celebrating` で移動を止め tween 競合を回避)。`animal_manager` が 7〜15 秒間隔で近くの子を1匹選んで歌わせ、slug で旋律を固定(同じ子はいつも同じ歌)。4種のペンタトニック・フレーズ。
- **パンタグラフ上下動(B4)**(`train.gd`): `_attach_pantograph` のアーム・集電板を `_panto[]` に保持し、`_process` で sin により上下に微動(走行中は少し高め・停車中はわずかに下げる)。視覚のみ・負荷軽微。
- **草の揺れ(B2)**(新規 `assets/shaders/grass.gdshader` + `scripts/world/grass.gd`): 中央エリア(原点半径95m・草原のみ・湖や雪山は除外)に MultiMesh(**1 draw call**)で約1400株。`grass.gdshader` の頂点シェーダーが上端ほど風で揺らす(下端固定、株ごとに位相をずらす)。NORMAL を上向き固定で地面と同じ陰影=夜は自然に暗くなる。Compatibility 対応。**密度・範囲は iPad 実機の fps を見て調整予定**(現状は安全側に控えめ)。

検証: 全スクリプト構文チェック通過、Main.tscn ヘッドレス起動でエラー・配線・signal 接続/解除の警告なし、AutoCapture で前面展望(レールが前方へ伸びる)・窓ぎわ(車体+パンタグラフ)・草の描画(シェーダーエラーなし)を確認。`for_mobile=false` 維持。
**実機確認待ち(改善さん iPad)**: ①「ながめ」3視点の切替が酔わないか/前面展望が楽しいか ②車内アナウンス・発車チャイムが鳴るか ③動物の歌が聞こえるか ④パンタの動きが自然か ⑤**草の fps 影響**(最重要・密度上限の判断)。

## v0.25.0 — 2026-06-01 — 作りこみ本格化 第1ウェーブ(にぎやか/見た目/電車)

Phase 6「体験を深める」。改善さんが選んだ3方向「世界をにぎやかに・見た目を磨く・電車を深く」を、独立デプロイ可能なウェーブに分けて実装。第1ウェーブは効果が高く実装が固い5点。

- **電車の車輪回転(B3)**(`train.gd`): 走行に応じて車輪がころころ回る。子供が一番見るところ。`_build_wheel` の車輪参照を `_wheels[]` に保持し、前進量 `advance / WHEEL_RADIUS` を車軸(ローカルY)まわりに回転。負荷対策でカメラから 110m 以内の編成だけ回す近接ゲート。
- **駅メロ(C1)**(`train.gd` + `station_manager.gd`): 電車が駅に着いた瞬間、その駅テーマの短メロが鳴る。`train.gd` に `arrived(anchor_pos)` シグナルを追加(dwell 到着時・位置確定後に発火)。`station_manager` が受けて最寄り駅を特定し、駅ごとのペンタトニック旋律(`_make_jingle` で正弦波合成)を再生。カメラ75m以内の編成のみ・クールダウン1.2秒で多重再生を防止。6駅ぶんの旋律を内蔵。
- **トゥーンシェーディング(B1)**(`assets/shaders/rim.gdshader`): 既存リムライトに `light()` を追加し、ハーフランバートを `toon_steps`(既定3段)に量子化して絵本のようなセル調に。動物・プレイヤーが共有する1シェーダーなので一括反映。境目は `toon_softness` で軽く和らげ、陰はハーフランバート+環境光で真っ黒にしない(怖くない)。Compatibility(WebGL2)で動く範囲の単純な light()。
- **駅のお団子で げんきアップ(A1)**(`station.gd`/`station_manager.gd`/`game_state.gd`/`save_system.gd`/`sound_fx.gd`/`player.gd`/HUD): おかし駅の三色だんごに近づくと「おだんご もぐもぐ! げんき アップ!」。`GameState.energy`(新規・保存対応)が +1、さくら色のキラキラがはじけ、プレイヤーがぴょこっと喜び(`player.celebrate()`)、やさしい「もぐもぐ音」。HUD トップバーに緑の「げんき」カウンタを追加。連続取得しない5秒クールダウン。近接トリガー方式(タッチ不要)。
- **流れ星で おねがいごと(A2)**(新規 `scripts/fx/shooting_star.gd`、Main 直下): 夜にときどき、プレイヤー上空を光の尾がスーッと横切り「ながれぼし! おねがいごと…」のテロップ。出現はランダム間隔(9〜22秒)。地上の集める星とは別の空の演出。`transparency` Tween でフェード、one-shot で自動解放。

検証: 全スクリプト構文チェック通過、Main.tscn ヘッドレス起動でエラー・配線警告なし、トゥーンシェーダーは朝昼夕夜の4スクショで正常描画(夜も暗すぎず)・HUD「げんき」表示確認。`export_presets.cfg` の `for_mobile=false` 維持。
**実機確認待ち(改善さん iPad)**: ①車輪の回る向きが自然か ②おだんごが食べられるか ③駅メロ・もぐもぐ音が鳴るか(iPad Safari の AudioContext)④夜に流れ星が見えるか ⑤げんきカウンタが読みやすいか。

## v0.24.0 — 2026-05-31 — iPad 実機フィードバック対応(文字化け/電車停止/UI/操作/降車)

改善さんに iPad・スマホ実機で遊んでもらい、出た不具合を順次修正・デプロイ。

- **日本語フォント埋め込み**(文字化け解消): `assets/fonts/` に M PLUS Rounded 1c(本文・既定 `gui/theme/custom_font`)+ Mochiy Pop One(見出し)。Label3D(駅看板・♥)は `station.gd`/`animal.gd` で個別 `font` 指定。Web には日本語システムフォントが無く豆腐化していた(PC エディタ実行は OS フォント補完で出ていた)。
- **電車を `_physics_process` 化**(`train.gd`): プレイヤーは物理(固定60Hz)、電車は描画 `_process` だったため、iPad の低fpsで電車だけ止まって見えた。物理プロセス化で低fpsでも時間どおり走行。初期化を再試行式に堅牢化。
- **ベース解像度 1920×1080 → 1024×576**(`project.godot`): UI が小さく操作しにくい対策(約1.9倍大・描画も軽量化)。
- **セキュリティ強化**: `vercel.json` に CSP/Permissions-Policy/X-Frame-Options(DENY)/Referrer-Policy/X-Content-Type-Options/X-Robots-Tag。`robots.txt`+`noindex` で検索除外。`CLAUDE.md` に「セキュリティ原則(子供向け配信)」追加。
- **実機4点**: ①「ミッション クリア!」通知をミッション表示と分離(中央へ移動)②ドクターイエローを park→dwell(走行)③動物の向きを進行方向へ正対(`rotation.y=_heading+PI`)④カメラ手動回転ボタン(`camera_rig.rotate_view` 45°ずつ・なめらか、TouchHUD に緑「カメラ ◀ ▶」)。
- **降車できない不具合**: タッチの「おりる」が `interact` action のエッジ検出で、タッチ/Web の指離し取りこぼしで押されっぱなしになり2回目が効かなかった。`Button.pressed` 直結 → `RideController.toggle_ride()` 方式へ(キーボードは interact 経由で維持)。
- ⚠️ `export_presets.cfg` の `vram_texture_compression/for_mobile` は `false` 必須(`true` だと Web export が configuration error で失敗)。`--headless --editor` 実行で勝手に `true` 化することがあるので export 前に要確認。
- 一時的に診断用 `[DBG Train]` print(`train.gd`)と `export/web/dbg.html` を残置。動作確定後に除去予定。

## v0.23.0 — 2026-05-31 — 広域マップ + 分岐つき線路網(列車の重なり/数珠つなぎ解消)

改善さん要望「世界を広く・線路網を複雑化・各編成に止まる場所・自動分岐で数珠つなぎ防止」への対応。7 フェーズで実装。

- **Phase 1 地形拡大**: `WORLD_SIZE 400→700`, `MESH_SUBDIV 120→180`(頂点 ~33k), `COLLISION_SAMPLE_STEP 2.0→3.0`。山・湖を外周へ再配置。フォグ密度 0.002→0.0009。
- **Phase 2 railway 一般化**: 単一楕円前提 → 任意の Curve3D 駆動(`_build_curve_from_waypoints`/`_build_rails_for`/`_build_ties_for`、`sample_baked` + 弧長間隔)。
- **Phase 3+4 線路網 + 列車ルート専用化**: `scripts/world/route_data.gd` で 9 ルートをデータ駆動定義。**各編成に専用の閉ループ**(独立 Curve3D/Path3D)→ 曲線を共有しないので速度差があっても**構造的に衝突・数珠つなぎゼロ**。train に状態機械 RUNNING/DWELLING/PARKED(dwell=数秒停車→再発車, park=車庫待機, `depart()` で解除)。`get_route_path/get_route_stops/get_route_start_offset` 公開。
- **レイアウト刷新**(改善さん要望 名所/枝分かれ/立体交差/本線形状): 波打つ 3 車線本線 + 名所ループ(湖 SL=自動橋脚 / 山B つばさ / 街 やまのて)+ **高架の立体交差**(つばめ +8m、橋脚を自動生成・ルート毎 MultiMesh)。
- **Phase 5 配置物移行**: StationData に route_slug/route_ratio、`railway.get_route_sample(slug, ratio)` で駅を停車点へ。6 駅割当(みどり→はやぶさ等)。街を広域クリア地へ(メイン街は やまのて線の内側)、踏切を地表ループの線路上へ。トンネルを つばさルート(山B)に作り直し。
- **Phase 6 空間配置物**: 雲 18→28(±340)、星 12→18(半径120)。乗車(屋根上視点)が新本線で正常動作を確認。撮影視点を新座標へ。
- **Phase 7 整理**: 旧 `ellipse_point/ellipse_tangent/get_track_path/_ellipse_waypoints` と関連定数・未使用 import を全廃。

### 検証
- パースエラーなし、9 編成正常走行(BIRD/SIDE/TRAIN_CLOSE/STATION/TOWN/TUNNEL/LAKE/AUTO_RIDE で確認)
- 各編成=専用曲線 + 交差は高さ分離 → 重なり/数珠つなぎが起きないことを構造的に保証
- みどり駅で はやぶさが停車・屋根上視点 OK・高架の立体交差/橋脚 OK・トンネル OK・湖ループ OK

### メモ
- 「枝分かれ」は衝突回避のため**別ルートが分岐・合流して見える**表現(実体は専用曲線)。プレイヤーが切り替える運転手機能は将来。
- 橋脚は線路面が地面より 2.5m 以上高い所に自動生成(高架・水上)。
- 旧 Railway.tscn の $TrackPath/$Rails/$Ties は未使用(空)で残置。

次のステップ: 改善さんの実機確認(広さ・動作の重さ・線路網の見た目)→ OK ならビルド成果物コミット。

## v0.22.1 — 2026-05-31 — 編成を車両ごとに屈折(改善さん指摘: 坂で先頭車が浮く)

改善さん「1車両ごとにばらけていないため、坂をのぼると1車両目が宙に浮く」への対応(v0.22.0 の本質的な原因)。

- 旧: 5 両編成をひとつの剛体(Node3D)として**編成中央の 1 点(PathFollow3D)**にぶら下げていた。坂・カーブでは中央の傾きに固定されるため、先頭車・末尾車が線路から浮く/めり込む
- 新: **各車両・各連結部をそれぞれ独立した PathFollow3D に載せ**、編成中心からの弧長オフセット(`-car_center_z`、先頭は正)で配置(`scripts/entities/train.gd` `_build_cars` / `_apply_progress` / `_make_follow`)。各車両が自分の位置の線路に沿って屈折 → 坂の稜線で編成が「く」の字に曲がり、どの車両も線路に乗る
- 中央 PathFollow(`_path_follow`)は見た目を持たず、乗車カメラ/アンカー専用に維持(`get_ride_*` は不変)
- ノード数: 1 編成あたり PathFollow が 1(中央)+ 5(車両)+ 4(連結部)= 10。×9 編成 = 90。progress 更新は軽量

### 検証

- パースエラーなし、9 編成正常走行
- TUNNEL ビュー(山の坂)で編成が稜線に沿って屈折し、先頭車の浮きが解消したのを確認
- 平地では各パートが旧剛体と同じ弧長位置に来るため見た目は不変(連結維持)

次のステップ: 改善さんに実機確認(坂・カーブでの編成の屈折/滑らかさ)→ OK ならビルド成果物コミット

## v0.22.0 — 2026-05-31 — 列車の動きを滑らかに(改善さん要望: かくつき・高低差での挙動)

改善さん「電車がかくつく。特に高低差がある場所であり得ない挙動」への対応。原因は 2 つ:

- **(主因)速度サージ**: 旧実装は `progress_ratio = 角度t / TAU`。`t` は楕円の角度で、`progress_ratio` は弧長(実距離)割合。坂を登る区間は「角度あたりの実距離」が増えるため、列車が**坂で急加速→坂を越えると急減速**していた(本来は逆)。→ `scripts/entities/train.gd` を**弧長ベース**に変更: `_path_follow.progress`(メートル)を `_linear_speed`(= 旧角速度を周回時間が一致するよう換算)で等速に進める。線路上のどこでも一定速度に
- **折れ線カーブ**: 旧 `_build_track_path` は 200 頂点を直線でつなぐだけ → ROTATION_ORIENTED の向きが頂点ごとにカクッと切替。→ `scripts/world/railway.gd` で **Catmull-Rom 風の接線(handle)**を各点に付け、滑らかな閉曲線に。レール実体メッシュとの差は数 cm で不可視
- 駅停車の減速も角度判定から**弧長オフセット判定**(`get_closest_offset` で事前計算)に置換。駅近傍で 25% まで減速する挙動は維持

### 検証

- パースエラーなし、9 編成が正常走行
- TUNNEL ビュー(山B の坂区間)で列車が線路にぴったり乗り、なめらかに山を越えるのを確認
- 列車は線路から浮き/埋もれなし

### メモ

- `train.gd` の static `_station_slow_factor(t)` は弧長版 `_slow_factor_at(progress)`(インスタンスメソッド)に置換
- 体感(等速・サージ解消・滑らかな旋回)は実機 60/30fps で要確認

次のステップ: 改善さんに実機確認(滑らかさ・重さ)→ OK ならビルド成果物コミット

## v0.21.0 — 2026-05-30 — 線路の見どころ(鉄橋・トンネル・改善さん要望)

改善さん「線路の見どころ(鉄橋・トンネル)」への対応。`scripts/world/landmark.gd` + Main の Landmarks ノード新規。

- **鉄橋**: 湖を渡る区間(線路 t 1.85〜2.18)に赤いトラス橋 — 橋脚(湖底から線路面へ)+ 左右の主桁・上弦・斜材。`_rail_y` で湖上は水面以上の線路高さを再現。電車が湖を渡る絵に
- **トンネル**: 山B を抜ける区間(t 3.62〜3.98)にレンガの坑口×2(柱+梁+石アーチ)+ かまぼこ天井・側壁。電車がくぐる
- 配置は `Railway.ellipse_point` / `ellipse_tangent` で線路に正確に沿わせ、向きも tangent に合わせた
- `scripts/dev/auto_capture.gd` — `ViewMode.TUNNEL` を追加(鉄橋は LAKE ビューで確認)

### 検証

- パースエラーなし
- LAKE ビューで赤いトラス鉄橋+橋脚、電車が湖を渡るのを確認
- TUNNEL ビューでレンガ坑口+天井、電車がくぐるのを確認

### メモ

- トンネルは山の斜面を覆う形(地形を貫通する穴ではない)。坑口+天井で「トンネルを抜ける」演出
- 天井は坑口間に数枚(隙間あり)。連続させたい場合は枚数を増やす(負荷とトレードオフ)

次のステップ: 改善さんに実機確認(鉄橋・トンネル・動作の重さ)→ 道路網 / 親モード / 季節 など

## v0.20.0 — 2026-05-30 — 街の作り込み(複数集落・広場・踏切・改善さん要望)

改善さん「道路・駅前広場・街灯・ベンチ・噴水」「街を複数に(駅ごとに集落)」「線路の踏切」への対応。`scripts/world/town.gd` を全面拡張。

- **複数集落**: メインの街(中央 (46,-26)に 9 棟+広場)に加え、6 駅それぞれのそば(駅から内側 26m)に小集落(3 棟+街灯+ベンチ)。`Railway.ellipse_point(track_t)` で駅位置を取得し配置
- **駅前広場**: `_build_plaza` — 舗装の円、噴水(下水盤+中央柱+上皿、水は半透明で少し光る)、ベンチ×2、街灯×2
- **街灯**: ポール+光る玉(UNSHADED 黄、夜は Glow で街灯に)。**ベンチ**: 座面+背+脚。**街路樹**を集落に散らす
- **踏切**: `_build_crossing` — 線路の 3 箇所(track_t 0.55/2.65/4.75)に、道(線路を横切る板)+ 両脇の警報機(赤い光るライト+ポール)+ 黄色い遮断機バー。`ellipse_tangent` で線路の向きに合わせて配置
- `scripts/dev/auto_capture.gd` — `ViewMode.TOWN` の look 位置を新メイン街中心に調整

### 検証

- パースエラーなし(集落・広場・噴水・街灯・踏切すべて生成 OK)
- BIRD で集落が線路沿いに点在するのを確認
- TOWN で建物が密集したメインの街並み(噴水・街灯含む)を確認

### メモ

- 建物・小物が増えたので動作の重さは実機で要確認(重ければ town.gd の集落数/棟数/窓数を調整)
- 集落間をつなぐ道路網はまだ簡易(広場の舗装のみ)。本格的な道路は今後

次のステップ: 改善さんに実機確認(街の賑わい・踏切・動作の重さ)→ 道路網 / 親モード / 季節 など

## v0.19.0 — 2026-05-30 — オープンワールド化(街 + 線路拡大・改善さん要望)

改善さん「オープンワールドなので世界観を出して。線路の拡張と街を作成」への対応。

- **街(world)**: `scripts/world/town.gd` + `scenes/world/Town.tscn` 新規 — 線路内側の区画(ORIGIN 18,-48 ~)に建物群をグリッド配置。
  - 家(パステル壁+三角屋根 PrismMesh+ドア+光る窓)/ お店(日よけ+看板+大窓)/ ビル(3〜4階+窓格子)/ 街路樹。中央列を「通り」として空ける
  - 窓は UNSHADED 水色で夜は Glow で街の灯りに。配置は seed 固定。各建物は地形高さに settle
- **線路拡大**: `scripts/world/railway.gd` の `TRACK_R_X` 100→135 / `TRACK_R_Z` 78→105、`TRACK_SEGMENTS` 157→200。楕円が地形いっぱいに広がり世界が広く。駅・電車・降車位置・虹・星は `ellipse_point` 連動で自動追従(角度ベースの駅 track_t も不変)
- `scenes/Main.tscn` — Town を配置
- `scripts/dev/auto_capture.gd` — `ViewMode.TOWN`(街を斜め上から)を追加

### 検証

- パースエラーなし(town の建物生成 OK)
- BIRD で線路が拡大し地形いっぱいに広がるのを確認
- TOWN ビューでカラフルな家・お店・ビル・街路樹・光る窓のかわいい街並みを確認

### メモ

- オーディオ(BGM)で WASAPI 警告が出ることがあるが PC 環境依存・無害(Web 実機は別)
- 線路は楕円のまま拡大(電車システムが Path3D 1 本前提のため)。支線・分岐は将来の大改修
- 街の規模・位置・建物の種類は調整可能(town.gd の ORIGIN / GRID / 配色)

次のステップ: 改善さんに実機確認(街の雰囲気・広さ)→ 街の作り込み(道・駅前広場)/ 親モード / 季節 など

## v0.18.1 — 2026-05-30 — カメラ固定に戻す + 目をかわいく(改善さんフィードバック)

改善さん「カメラは固定でいい(画面酔いしそう)」「目をもっとかわいく」への対応。

- **カメラ固定化**: `scripts/world/camera_rig.gd` から orbit のドラッグ/キー入力を撤去し、固定アングル(yaw0 / pitch0.5)の滑らか追従に。`scripts/main.gd` の InputMap を元に戻す(矢印キー=移動、cam_* は削除)
- **目の改善**: `scripts/entities/player.gd` の目を、白目を縦長に+黒目を大きく(うるうるの瞳)+キラキラ 2 つ(大きいの上・小さいの下)に。アニメキャラのような目になった
- `scripts/dev/auto_capture.gd` は CHAR ビューで確認後 PLAYER に戻し

### 検証

- CHAR ビューで大きなうるうる目+キラキラを確認。とてもかわいい
- カメラは固定追従(orbit 廃止)でスクリプトエラーなし

次のステップ: 改善さんに実機確認 → 親モード / 季節 / 自由アイデア

## v0.18.0 — 2026-05-30 — プレイヤー刷新(かわいい運転士)+ カメラ移動(改善さん要望)

改善さん「人物をもっとかわいく、本気で」「カメラの移動も可能に」への対応。

- **プレイヤー刷新**: `scripts/entities/player.gd` を大改修 — 3 頭身のかわいい「うんてんしさん」をスクリプト生成。青い運転士帽子(クラウン+帯+つば+金エンブレム)、大きい目(白目+黒目+ハイライト)、ピンクのほっぺ、ちょこんと鼻、黄色いシャツ+青えり、肌色の腕+丸い手、青ズボン+茶靴、後ろ髪。全パーツにリムライト
  - **歩行アニメ**: 移動中は腕・足を支点(肩/股)で交互に振り、体が上下にぴょこぴょこ。止まると lerp で戻る
  - `scenes/entities/Player.tscn` は CharacterBody3D + CollisionShape のみに簡素化(見た目は全部スクリプト生成)
- **カメラ移動**: `scripts/world/camera_rig.gd` を orbit 化 — yaw(水平回転)/ pitch(見上げ・見下ろし)を入力で変える。距離固定で滑らかに追従
  - タッチ: 何もない所のドラッグで見回す(`InputEventScreenDrag`。D-pad/ボタンは HUD が吸収)
  - PC: 矢印キー(`cam_*`)またはマウス左ドラッグ
  - `scripts/main.gd` の InputMap — 移動を WASD のみにし、矢印キーを `cam_left/right/up/down` に割当
  - `scenes/Main.tscn` — CameraRig の旧 `offset` を除去、smoothness 8.0
- `scripts/dev/auto_capture.gd` — `ViewMode.CHAR`(プレイヤーの顔を斜め前から撮る)を追加

### 検証

- パースエラーなし
- CHAR ビューで新プレイヤーの顔(帽子・目・ハイライト・ほっぺ・シャツ・靴)を確認。とてもかわいい仕上がり
- PLAYER ビューで orbit カメラがプレイヤー後方上から追従(初期 yaw0/pitch0.5)することを確認
- 歩行アニメ・カメラのドラッグ/キー回転は入力を伴うため実機確認向き(ロジックは単純)

次のステップ: 改善さんに実機確認(キャラのかわいさ・歩く様子・カメラを指で回せるか)→ 親モード / 季節 / 自由アイデア

## v0.17.0 — 2026-05-30 — Phase 5(BGM・進捗保存・PWA)

「親モード以外ぜんぶ」の依頼で、BGM・セーブ・PWA をまとめて実装。

- **BGM(4-4)**: `scripts/world/bgm.gd` 新規(Main 直下)— ペンタトニックのゆったり旋律をスクリプト生成(`AudioStreamWAV`、`LOOP_FORWARD` でループ)。基音+1オクターブを混ぜたやさしい音色、低音量(-16dB)。タイトルの「はじめる」後に鳴り始める
- **進捗保存(5-6)**: `scripts/world/save_system.gd` 新規(Main 直下、GameState の直後に配置)— GameState を `user://save.json` に JSON 保存/読込(Web は IndexedDB に永続)。`changed` で保存、起動時に読込。読込は emit せず値だけセットし、各 listener が `_ready` で現在値を反映 → 起動時の効果音誤発火を防止。star/boarded/befriended/stations を復元
- **PWA化(5-1/5-5)**: `export/web/manifest.json` 新規(name/short_name/fullscreen/landscape/色/apple-touch アイコン)。`web/template.html` に `<link rel="manifest">` / `apple-touch-icon` / `icon` を追加。既存の apple-mobile-web-app-capable と合わせ、iPad で「ホーム画面に追加」→ フルスクリーン起動できる
- `scenes/Main.tscn` — SaveSystem(GameState 直後)/ Bgm を配置
- `scripts/world/mission_manager.gd` 修正 — HUD は後から `_ready` するため、ミッション表示を `call_deferred` で遅延反映(ロード後の現在ミッションが正しく出る)
- `scripts/world/sound_fx.gd` 修正 — `_ready` で prev カウントを現在値に初期化(ロード後の誤発火防止)

### 検証

- パースエラーなし(BGM の WAV 生成、SaveSystem、PWA は CLI 無関係)
- 1 回目(AUTO_BOOK)で進捗を保存 → 2 回目(SINGLE)起動で「ほし 3 / なかよし 2」とミッション「ほしを 6こ あつめよう」(4 つ達成済みをスキップ)が復元されることを確認
- 修正: ミッション表示が空 → MissionManager が TouchHUD より先に `_ready` し HUD ラベルが未初期化だった。`call_deferred` で解決

### メモ(実機確認)

- BGM / 効果音は Web では「はじめる」(最初の操作)後に鳴る
- 完全オフライン化(Service Worker キャッシュ)は Godot の Web Export ダイアログで PWA を ON にするか、今後 SW を追加。現状でも manifest + apple-touch で iPad のホーム追加・フルスクリーンは可能
- 進捗リセットは将来の親モードで(今は save.json 削除で初期化)

次のステップ: 改善さんに実機確認(はじめる→音/BGM、ホーム追加、進捗が次回続くか)→ 親モード / さらなる季節 / 自由アイデア

## v0.16.0 — 2026-05-30 — スタート画面(Phase 5 入口・音の起こし)

- `scenes/ui/TitleScreen.tscn` + `scripts/ui/title.gd` 新規 — 「しんかんせんワールド」タイトル+「でんしゃで あそぼう!」+ 黄色い「はじめる」ボタン。背景に world が薄く透ける(空色ベール 0.82)。CanvasLayer layer=3 で最前面
  - 「はじめる」押下が **最初のユーザー操作**になり、ブラウザの AudioContext が有効化される(効果音が鳴るようになる)。押下で Root を alpha フェードアウト → visible=false でプレイ開始
- `scenes/Main.tscn` — TitleScreen を最前面に配置
- `scripts/dev/auto_capture.gd` — 検証スクショではタイトルを隠す処理を `_ready` 冒頭に追加

### 修正したハマり

- TitleScreen.tscn の Root ノードに `parent="."` を書き忘れ → 「Invalid scene: node Root does not specify its parent node」でシーン全体がロード失敗(GL/RID リーク多発)。最初のノード以外は parent 指定が必須。修正後はエラーなし

### 検証

- タイトル画面の表示(タイトル・サブ・はじめるボタン・背景の world 透け)を確認。スクリプトエラーなし
- 「はじめる」のフェードアウトはロジック単純(Tween)。音が鳴るかは Web 実機で要確認

### 今後(Phase 5 残り)

- BGM(穏やかな曲)、進捗の LocalStorage 保存、manifest.json / Service Worker の PWA 化、親モード

次のステップ: 改善さんに実機で「はじめる→音が鳴るか」を確認 → BGM / 保存 / PWA へ

## v0.15.0 — 2026-05-30 — Phase 4 続き(リムライト・季節・ミッション・音)

「ぜんぶ続けて」の依頼で、残りの作り込みをまとめて実装。

- **リムライトシェーダー(4-1)**: `assets/shaders/rim.gdshader` 新規 — 輪郭(視線に斜めの面)が `rim_color` で光るやさしいリム。`albedo` を uniform で受けて 1 つのシェーダーを使い回す。`scripts/entities/animal.gd` の `_mi`(shaded メッシュ)と `scripts/entities/player.gd`(Body/Head/Nose)に適用。Glow と相乗でふんわり光る
- **季節演出(4-2/6-3)**:
  - 夜のホタル: `scripts/fx/fireflies.gd` + `scenes/fx/Fireflies.tscn` 新規 — Player の子に GPUParticles3D、黄緑の光が周囲をふわふわ。`day_night` 連動で夜のみ emitting。color_ramp で点滅
  - 雨上がりの虹: `scripts/world/rainbow.gd` + `scenes/world/Rainbow.tscn` 新規 — 7 色の TorusMesh アーチを遠景 (0,2,-185) に。半透明+emission。`Main.tscn` に配置
- **ミッション(3-6)**: `scripts/world/mission_manager.gd` 新規(Main 直下)— 5 段階のやさしいおつかい(でんしゃに乗る→なかよし→ほし3→えき発見→ほし6)。`GameState.changed` で達成判定し順に進む。失敗・制限時間なし。HUD 上部中央に「ミッション: ○○」表示(`touch_hud.set_mission` + TouchHUD の Mission ラベル)、クリアで通知
- **効果音(4-4)**: `scripts/world/sound_fx.gd` 新規(Main 直下)— 正弦波の短い 2 音をスクリプト生成(`AudioStreamWAV`、素材ファイル不要)。星・なかよし・乗車・駅発見で違う音。`GameState.changed` で増分検知して再生
- `scenes/Main.tscn` — MissionManager / SoundFX / Rainbow / Fireflies を配置・配線

### 見送り

- **列車の車輪回転**: 9 編成 × 5 両 × 8 輪 ≈ 360 個を毎フレーム回す負荷が「かくかく」を悪化させる懸念があり見送り。MultiMesh 化や LOD 整備の後に再検討

### 検証

- パースエラーなし(rim シェーダー、ホタル/虹の GPUParticles・TorusMesh、ミッション、WAV 生成すべて OK)
- 夜の FOUR_TIMES で ホタル(黄緑の光点)+ 虹アーチ全体 + 星・窓の Glow を確認
- AUTO_BOOK で「ミッション: ほしを 6こ あつめよう」(4 つクリア済みで 5 番目を表示)+ 図鑑 + カウンターを確認

### メモ(実機確認したい点)

- 音は Web の AudioContext がタッチ後に有効化されるため、最初の操作前は鳴らない可能性(スタート画面での起こし対応は Phase 5)
- Glow / リムライトの WebGL2 での効き具合

次のステップ: 改善さんに体験確認 → スタート画面 + 音の起こし(Phase 5)/ BGM / さらなる季節(紅葉・雪)/ 自由アイデア

## v0.14.0 — 2026-05-30 — Phase 4 演出の作り込み(光・キラキラ・動き)

「光って、はじけて、ふわふわする」表現を追加して世界をリッチに。Compatibility レンダラーで効く範囲に絞った。

- **Glow(4-6)**: `scenes/Main.tscn` の Environment に glow を追加(intensity 1.0 / strength 1.1 / bloom 0.15 / threshold 0.9)。星・窓・ヘッドライト・蒸気が柔らかく光る。DayNightCycle の毎フレーム上書きとは干渉しない
- **星のきらきら + 獲得バースト(4-2)**: `scripts/world/stars.gd` — 星ごとに material を複製して emission を `sin` で脈動。獲得時に `_spawn_burst()` で GPUParticles3D の星屑を一発はじけさせ、寿命後に自動削除(`finished` → `queue_free`)
- **なかよしハート(4-3/4-5)**: `scripts/entities/animal.gd` の `befriend()` で `_pop_heart()` を呼び、頭上に「♥」(Label3D, Y ビルボード, 白アウトライン)をふわっと出して上昇フェード
- **SL の蒸気(4-2)**: `scripts/entities/train.gd` — `has_steam` の編成(SL人吉)の煙突に `_attach_steam()` で GPUParticles3D の蒸気。`scale_curve`(CurveTexture)で上昇しながら拡大、白い半透明ふわふわ
- **UI ボタンのぷにっと(4-5)**: `scripts/ui/touch_hud.gd` — D-pad / ジャンプ / タッチ / ずかん ボタンに押下バウンス(button_down で 0.88 倍、button_up で 1.0 倍、`ease_out_back`)
- `scripts/dev/auto_capture.gd` — `ViewMode.STEAM`(SL の蒸気を撮る)を追加

### 検証

- 通常起動でパースエラーなし。星が Glow で淡く光ることを確認
- AUTO_BEFRIEND で頭上にピンクの「♥」+「うさぎと なかよし!」+ カウンター更新を確認
- STEAM ビューで SL人吉の煙突から白い蒸気がもくもく、窓・蒸気が Glow で光るのを確認(奥に にじ駅)

### メモ

- Glow は Compatibility/CLI では効いた。Web Export(WebGL2)での効き具合は実機確認が望ましい。効きすぎ/効かない場合は intensity / threshold を調整
- 未実装(今後): トゥーン/リムライトシェーダー、草の揺れ、列車の車輪回転・パンタ上下、音(BGM・効果音)、季節パーティクル(紅葉・雪・ホタル)

次のステップ: 改善さんに体験確認(特に Glow の効き)→ 音 or さらなるシェーダー or 季節 へ

## v0.13.0 — 2026-05-30 — Phase 3-3/3-4/3-5 + 2-4(星あつめ・HUD カウンター・駅停車・図鑑)

「集める・出会う・乗る・探す」の主要ループが一通り動くようになった。進捗を一元管理する GameState を土台に、星あつめ・カウンター・図鑑・駅停車をまとめて実装。

- `scripts/world/game_state.gd` 新規 — Main 直下の進捗集約ノード(Autoload 不使用方針)。`boarded_trains` / `befriended_animals` / `visited_stations` / `star_count` と `signal changed`、`add_*` / `has_*`。セッション内メモリ保持(永続セーブは Phase 5)
- **星あつめ(3-3)**: `scripts/world/stars.gd` を全面改修 — 夜のみ表示 → 常時きらきら浮遊・回転。プレイヤーが `GET_RANGE=2.6m` 内に近づくと獲得(動物なかよしと同じ近接自動方式)。獲得で `GameState.add_star` + HUD 通知「ほしを ゲット!」+ ぴょこっと拡大して飛んで消える Tween 演出
- **HUD カウンター + ずかんボタン(3-4)**: `scenes/ui/TouchHUD.tscn` + `scripts/ui/touch_hud.gd` に「ほし N」「なかよし N」カウンター(上部左)と「ずかん」ボタン(上部右)を追加。`GameState.changed` で更新、ボタンで図鑑を開く
- **図鑑(3-5)**: `scenes/ui/BookOverlay.tscn` + `scripts/ui/book.gd` 新規 — でんしゃ/どうぶつ/えき の 3 タブ。`resources/*_data/` の .tres を `DirAccess` で走査してマスター化し、`GameState` の発見状態と照合。発見済み=色見本+名前、未発見=「?」。開いている間は `get_tree().paused`(本 CanvasLayer は `process_mode=Always` で操作可)
- **駅停車(2-4)**: `scripts/entities/train.gd` に `_station_slow_factor()` 追加 — 駅の `track_t` 近傍(±0.22rad)で速度係数を 25% まで落として通過、離れると 100% に戻る(static 純粋関数、複数駅は最小係数)
- **駅発見**: `scripts/world/station_manager.gd` 新規(Stations ノード)— プレイヤー近接(9m)で駅を発見 → `GameState.add_station` + 通知「○○ えき はっけん!」
- 連携追加: `ride_controller`(乗車で `add_boarded`)/ `animal_manager`(なかよしで `add_befriended`)が GameState に記録。`train` / `animal` / `station` に `get_slug()` 追加
- `scenes/Main.tscn` — `GameState` ノード、`BookOverlay` を追加、各システムに `game_state_path` 等を配線。`Stations` に station_manager を付与、`Stars` の export を player/game_state/hud に差し替え
- `scripts/dev/auto_capture.gd` — `CaptureMode.AUTO_BOOK`(GameState に発見を入れて図鑑を開き でんしゃ/どうぶつ タブを撮る検証フック)を追加

### 検証

- 通常起動(PLAYER)で全スクリプト パースエラーなし。HUD に「ほし 0 / なかよし 0」カウンターと「ずかん」ボタン、世界に集める星(黄色く浮遊)を確認
- AUTO_BOOK で図鑑を確認: でんしゃタブ「はやぶさ」発見(緑見本+名前)・他「?」、どうぶつタブ「ねこ」「うさぎ」発見・他「?」、カウンターが「ほし 3 / なかよし 2」に更新
- 星獲得・なかよし・駅発見は同一の `GameState.add_*` 経路。近接獲得ロジックは AUTO_BEFRIEND(なかよし通知)で実証済み

### 範囲外 / 今後

- 駅停車の実挙動・星獲得演出の体感は実機確認向き(スクリプトのみでは静止画にならない)
- 図鑑の詳細画面(説明文・最高速度)、ミッション(3-6)、進捗の永続セーブ(Phase 5)

次のステップ: 改善さんに体験確認 → Phase 3-6(ミッション)/ Phase 4(演出の作り込み)/ 季節などの自由アイデア

## v0.12.0 — 2026-05-30 — Phase 3-2(動物がふらふら歩く + なかよし)

世界に 8 種の動物が現れ、草原をぴょこぴょこ歩き回り、近づくと「なかよし」になるようになった。データ駆動。
なかよしは改善さんの選択で「近づくと自動」方式(タッチ不要=3歳児にやさしい、乗車の interact と競合しない)。

- `scripts/entities/animal_data.gd` 新規 — `class_name AnimalData` extends Resource。`display_name` / `slug` / `species` / `body_color` / `accent_color` / `belly_color` / `scale_factor` を `@export`
- `scripts/entities/animal.gd` 新規 — extends Node3D。
  - 見た目を species ごとにスクリプト生成(体+おなか+頭+目、+ 耳/鼻/くちばし/しっぽ)。丸っこいデフォルメ
  - 簡易ステートマシン(IDLE ⇄ WALK)で `_home`(初期位置)半径 8m 内をふらふら。`randf_range` で状態時間と向きをランダム化
  - 歩行中は進行方向へ滑らかに向き(`lerp_angle`)、`sin` で上下バウンス(ぴょこぴょこ)。地形高さに追従(衝突なし=子供向けにすり抜けOK)
- `scenes/entities/Animal.tscn` 新規 — Node3D + animal.gd だけ(見た目は全部スクリプト生成、AnimalData で変わるので静的化しない)
- `resources/animal_data/*.tres` 新規 8 個 — うさぎ(rabbit)/ くま(bear)/ きつね(fox)/ ねこ(cat)/ ぱんだ(panda)/ いぬ(dog)/ ぺんぎん(penguin)/ ぶた(pig)
- `scripts/world/animal_manager.gd` 新規 — `Animals` ノードに付与。プレイヤーが `BEFRIEND_RANGE = 3m` 内に近づいた未なかよしの動物を `befriend()` させ、HUD に「○○と なかよし!」通知。`signal befriended(display_name, total)`(将来の HUD カウンター / 図鑑用)。interact を使わないので乗車と競合しない
- `scripts/entities/animal.gd` 追記 — `befriend()`(立ち止まって `ease_out_back` → `bounce` でぴょんと喜ぶ)/ `is_befriended()` / `get_display_name()`。喜びジャンプ中(`_celebrating`)は移動・バウンスを止める
- `scenes/Main.tscn` 修正 — `Animals`(Node3D + animal_manager)を追加(player_path / hud_path 配線)、配下に 8 体を線路内側の草原に配置
- `scripts/dev/auto_capture.gd` 修正 — `ViewMode.ANIMAL`(造形確認)+ `CaptureMode.AUTO_BEFRIEND`(プレイヤーをうさぎ隣にテレポートして なかよし通知を撮る検証フック)を追加

### 検証

- AutoCapture(ANIMAL ビュー)で うさぎ を確認: 白い丸い体・ピンクの長い耳・丸尾のかわいいデフォルメ。奥に他の動物・電車。スクリプトエラーなし
- AutoCapture(AUTO_BEFRIEND)で「うさぎと なかよし!」通知が出ることを確認(プレイヤー近接 → 自動成立)
- 修正したハマり: `_home` は Vector2 なので `.z` 不可 → `.y`(z 座標相当)に修正(Parse Error: Cannot find member "z" in base "Vector2")

### 範囲外(今回は実装せず)

- なかよし数の HUD カウンター表示(Phase 3-4 HUD で。`signal befriended` は布石済み)
- 動物との会話・固有モーションの作り込み(Phase 4 演出)

次のステップ: 改善さんに体験確認 → Phase 2-4(駅停車)/ 3-3(星獲得)/ 3-4(HUD カウンター)/ 3-5(図鑑)へ

## v0.11.0 — 2026-05-30 — Phase 3-1(駅をつくる)

線路沿いに 6 つの駅が立ち、世界に「目的地」ができた。データ駆動(TrainData と同方針)。

- `scripts/world/station_data.gd` 新規 — `class_name StationData` extends Resource。`display_name` / `sub_text`(看板の大小)/ `slug` / `main_color` / `accent_color` / `decor_type` / `track_t`(楕円上の配置)を `@export`
- `scripts/world/station.gd` 新規 — extends Node3D。`_ready` で `Railway.ellipse_point(track_t)` から線路脇の位置を求め、`rotation.y` を線路接線に合わせて配置。プラットフォーム(色違い床+アクセント縁)+ 屋根(柱4本+屋根板)+ 看板(Label3D ×2、常に読める `BILLBOARD_FIXED_Y`+太アウトライン)+ 固有装飾を全部スクリプト生成
  - 湖の上の区間(`track_t` が湖の半径内)では線路と同様に `compute_water_y()` 以上へ持ち上げ、駅が沈まないようにした
  - 装飾 `decor_type`: tree(木)/ flower(花壇)/ mountain(雪山)/ lake(池)/ sweets(三色だんご)/ rainbow(3本アーチの虹)を `match` で組み立て
- `scenes/world/Station.tscn` 新規 — Node3D + station.gd だけ(中身は全部スクリプト生成、StationData で色・装飾が変わるので静的化しない)
- `resources/station_data/*.tres` 新規 6 個 — みどり(tree, t=0)/ はな(flower, t=1.05)/ みずうみ(lake, t=2.09 ・湖のほとり)/ やま(mountain, t=3.14)/ おかし(sweets, t=4.19)/ にじ(rainbow, t=5.24)。楕円を 6 等分しつつ、湖にかかる t≈2.09 を「みずうみ駅」に割り当て
- `scenes/Main.tscn` 修正 — `Stations`(Node3D)を追加、配下に 6 駅インスタンス(各 station_data を override)
- `scripts/dev/auto_capture.gd` 修正 — `ViewMode.STATION`(みどり駅を間近で撮影)を追加

### 検証

- AutoCapture(STATION ビュー)で みどり駅を確認: 緑屋根+白柱のプラットフォーム、「みどり」「もりの えき」の看板が読める、木の装飾、はやぶさが駅前走行。スクリプトエラーなし
- BIRD ビューで 6 駅が楕円線路沿いに分散配置、みずうみ駅が湖際に沈まず立っていることを確認
- 子供向け配慮: ひらがなのみ、明るいパステル、怖い要素なし、看板は常に正面を向き読める

### 範囲外(今回は実装せず)

- 駅停車(Phase 2-4): 駅近くで電車が減速 → 次の周回で追加候補
- 駅に降りる演出(乗車システムの降車を最寄り駅に寄せる)
- 駅の図鑑タブ(Phase 3-5)

次のステップ: 改善さんに見た目確認(看板を空中ビルボードのままにするか、立て看板にするか等)→ 駅停車 or Phase 3-2(動物)へ

## v0.10.0 — 2026-05-30 — Phase 2-5(電車に乗るシステム)

走行中の電車に乗って、屋根の上から見下ろす視点でいっしょに線路を旅できるようになった。
乗車視点は改善さんの選択(屋根の上から見下ろす)を採用。

- `scripts/world/ride_controller.gd` 新規 — Main 直下のノード(extends Node3D)。乗車状態の中核:
  - 状態機械 `enum State { WALKING, RIDING }`。`interact`(タッチ / E / Enter)のトグルで乗降
  - `is_action_just_pressed("interact")` の唯一の消費者(従来 touch_hud が action_press するだけで誰も消費していなかった)
  - 乗降直後の即トグルを防ぐ `TOGGLE_DEBOUNCE = 0.4s`
  - `_find_nearest_ridable()`: プレイヤーから `RIDE_RANGE = 14m` 以内で最寄りの編成を選ぶ(編成中央のワールド位置で判定)
  - 乗車カメラを電車の PathFollow3D(ROTATION_ORIENTED, -Z 進行方向)の子に **ローカル固定 transform** で生成 → 進行方向に追従しつつ一切揺れない。屋根上俯瞰(pos (0,6,6)・pitch -28°・fov 60)、`current=true` で切替、降車で元の CameraRig/Camera3D に戻す
  - フェード遷移: HUD の全画面 ColorRect を Tween(0→1→中点でカメラ/プレイヤー切替→1→0、片道 0.25s)で噛ませ、カメラ切替の瞬間を隠す(怖くない・酔わない)
  - 降車は「その場の線路脇に降りる」簡易版(駅未実装のため)。`_compute_landing()` で編成中央から進行方向の直交・楕円外側へ `LANDING_OFFSET = 6m` ずらし、`TerrainHeight.compute_height + 1.5` で地形に着地(電車・レール・枕木に重ならない)
  - 判定ロジック `_nearest_index` / `_compute_landing` は static 純粋関数(C# 移植配慮)
  - `signal boarded` / `alighted`(将来の図鑑連携用)
- `scripts/entities/train.gd` 修正 — public API 追加(既存挙動は不変): `get_ride_anchor_position()`(編成中央の現在ワールド位置)/ `get_ride_mount()`(PathFollow3D, カメラのぶら下げ先)/ `get_ride_forward()`(進行方向)/ `get_display_name()`
- `scripts/ui/touch_hud.gd` + `scenes/ui/TouchHUD.tscn` 修正 — 乗車 UI:
  - `Prompt`(Label): 「○○に のる?」を上部中央にパステル枠付きで表示
  - `Notice`(Label): 「○○に のったよ!」を `ease_out_back` バウンス + フェードで登場、1.6s 後にフェードアウト
  - `Fade`(全画面 ColorRect, alpha 0): 遷移演出用。`set_fade_alpha(a)` で RideController の Tween から駆動
  - `set_riding(bool)`: 乗車中はタッチボタンを「タッチ」↔「おりる」に出し分け、D-pad を `disabled` + 薄表示(動かせないことを明示)
- `scenes/Main.tscn` 修正 — `Main` 直下に `RideController` を追加、player/trains/camera_rig/hud の NodePath を配線
- `scripts/dev/auto_capture.gd` 修正 — `CaptureMode.AUTO_RIDE` 追加。最寄り電車に強制乗車して屋根上視点を撮影 → 降車後を撮影(`_do_board`/`_do_alight` を直接叩く検証フック、本番コードは汚さない)

### 検証

- AutoCapture(AUTO_RIDE)で乗車・降車を無人撮影。スクリプトエラーなし
- 乗車スクショ: 屋根の上から進行方向を見下ろす視点、編成と前方の線路が見える。D-pad 薄表示・「おりる」ボタン・「のったよ!」通知を確認
- 降車スクショ: 線路脇(編成中央から 6m 外側・地形+1.5m)に着地、カメラと UI が歩行状態に復帰。位置ログで player=(101.1, 1.84, 25.3) / anchor=(95.5, 0.75, 23.1)、xz 距離 ~6m・Y は電車と重ならないことを確認
- 型安全: `train.gd` / `touch_hud.gd` は class_name 非対応(CLI スキャン問題)のため、`ride_controller` 側で `preload` const を型注釈に使い静的解決(train_data.gd と同方針)

### 範囲外(今回は実装せず)

- 運転席視点への切替(将来 ROADMAP 7「車内視点モード」で追加候補)
- 駅停車(Phase 2-4)/ 駅に降りる演出(Phase 3 駅と同時)
- 列車運転(加減速操作)

次のステップ: 改善さんに PC ブラウザ / iPad で体験確認 → Phase 3(駅・動物・星・図鑑)へ

## v0.9.1 — 2026-05-29 — 列車のリアル化(改善さんフィードバック対応)

改善さん「もっとリアルな新幹線にできませんか?」のフィードバック対応。4 つの方向で改修。

- `scripts/entities/train.gd` 大幅改修:
  - **編成を 3 両 → 5 両**(LEAD + MID×3 + TAIL、総長 ~26m)
  - **窓を一本の長い帯 → 個別小窓**(先頭/末尾車 4 個、中間車 6 個、両側面)
  - **ノーズを CylinderMesh(円錐)に**:
    - sharp: top_radius=0.12、bottom_radius=0.92、長さ 3.2m(はやぶさ風ロングノーズ)
    - rounded: top_radius=0.55、bottom_radius=0.95、長さ 2.2m + 先端 SphereMesh(N700 風カモノハシ)
    - steam: 横向き CylinderMesh(ボイラー)+ 縦の煙突(SL 風)
  - **台車を追加**: 各車両 2 台(前後)、台車枠 BoxMesh + 車輪 4 個ずつ
  - **連結部を追加**: 車両と車両の間に小さな BoxMesh(暗灰)、ジャバラ感
  - **パンタグラフを「く」の字 + 集電板に改良**(従来の単一バーから 2 本アーム + 上の横長 BoxMesh へ)
- `scripts/dev/auto_capture.gd`: `TRAIN_CLOSE` ビューモード追加(Hayabusa initial_t=0 の楕円起点 (100, 0) を間近で撮影)

スクリーンショットで Hayabusa の側面・小窓・台車・連結部・5 両編成が確認できた。子供向けの「ふにふに感」とのバランスは保持(プリミティブの組み合わせのまま、Blender モデリングは導入せず)。

## v0.9.0 — 2026-05-29 — Phase 2-1 + 2-2 + 2-3(9 編成の新幹線が線路を走る)

- `scripts/entities/train_data.gd` 新規 — `class_name TrainData` extends Resource。9 項目(display_name / slug / body_color / accent_color / speed / nose_type / has_pantograph / has_steam / initial_t)を `@export`
- `scripts/entities/train.gd` 新規 — `_ready` で Railway の Path3D を取得し、PathFollow3D を動的 `add_child`。`_process` で t を進めて `progress_ratio = t / TAU` を更新。`_build_visual()` で 3 両編成(LeadCar + MidCar + TailCar)+ 各車両のパーツ(本体 / アクセント帯 / 窓帯 unshaded / 車輪 4 個 / ノーズ / ヘッドライト / パンタグラフ)を全部スクリプト生成
- `scenes/entities/Train.tscn` 新規 — Node3D + train.gd だけ(中身は全部スクリプト生成、TrainData で色や形が変わるので静的シーン化不可)
- `resources/train_data/*.tres` 新規 9 個 — はやぶさ / こまち / かがやき / N700 / ドクターイエロー / つばさ / つばめ / SL人吉 / E235やまのて(Three.js プロトタイプの色・速度・配置をそのまま移植)
- `scenes/Main.tscn` 修正 — `Trains (Node3D)` 追加、配下に 9 編成インスタンス。各 train_data + railway_path を override で指定
- `scripts/world/railway.gd` 修正 — `func get_track_path() -> Path3D` を公開 API として追加(Train から呼ばれる、ただし現状は NodePath で直接アクセスで動作)

### Three.js から移植した 9 編成

| 名前 | body | accent | nose | speed | initial_t |
|------|------|--------|------|-------|-----------|
| はやぶさ | 緑 #009944 | ピンク #ff6b9a | sharp | 0.13 | 0.0 |
| こまち | 赤 #ff5577 | 白 | sharp | 0.11 | 0.9 |
| かがやき | 白 #fafafa | 金 #c9a44d | sharp | 0.14 | 1.8 |
| N700 | 白 | 青 #0066cc | rounded | 0.12 | 2.7 |
| ドクターイエロー | 黄 #ffe066 | 青 | rounded | 0.09 | 3.6 |
| つばさ | 紫 #b6a4ff | 金 #ffd700 | sharp | 0.10 | 4.5 |
| つばめ | 紺 #222244 | 金 | sharp | 0.13 | 1.4 |
| SL人吉 | 黒 #1a1a1a | 赤 #c0392b | steam | 0.07 | 5.0 |
| E235やまのて | 黄緑 #b5e853 | 白 | rounded | 0.15 | 3.0 |

### 重要な設計判断

- **データ駆動**: 新編成追加は .tres を 1 つ作るだけ、コード変更不要
- **PathFollow3D を動的 add_child**: Train.gd の _ready で Railway.Path3D の子に PathFollow3D を作って自身の visual を add_child。9 編成全部が同じ Railway.Path3D を共有
- **メッシュは個別 MeshInstance3D**(9 編成 × 3 両 × ~10 パーツ = 約 270 個、Compatibility で問題ない数)
- **窓は UNSHADED**(夜でも水色に光る感)、本体・アクセントは shaded
- **衝突なし**: 列車同士はすり抜け(子供向けに「ぶつかった!」の悲しみを回避)

### 範囲外(今回は実装せず)

- Phase 2-4 駅停車(Phase 3 駅と同時)
- Phase 2-5 乗車システム(UI 大改修)
- SL の蒸気エフェクト / ヘッドライトの夜間光 / 車輪回転 / パンタグラフ上下動 → Phase 4 演出

次のステップ: Phase 3(駅・動物・星・図鑑)に着手

## v0.8.0 — 2026-05-29 — Phase 1-5(湖の water シェーダー)→ **Phase 1 完了**

- `assets/shaders/water.gdshader` 新規 — Godot Shading Language(GLSL ベース)。`shader_type spatial`、`render_mode cull_back, diffuse_lambert, specular_schlick_ggx`。頂点シェーダーで sin/cos 波(`wave_strength 0.08`、`wave_frequency 1.5`、`wave_speed 0.8`)、フラグメントで水色 albedo + 高スペキュラ(0.85)
- `scripts/world/terrain.gd` 修正 — `_generate_lake` 書き直し: `CylinderMesh` → `PlaneMesh + subdivide 32×32`(波計算用に細分化)、`StandardMaterial3D` → `ShaderMaterial(water.gdshader)`。湖サイズは `LAKE_RADIUS * 2 + 4`(山の壁で余分は隠れる)。水面 Y を `compute_height(中心) + 4.5` に上げて湖の谷の縁近くに配置(深い谷の底だと水面がほぼ地形に隠れる)

### 試行錯誤メモ

- **半透明 + フレネル**(初期案)→ 上から見ると透けすぎて地形が見えるだけで湖が認識できない → 不透明に妥協
- **円形 ArrayMesh**(自作)→ triangle fan は CCW で動くが ring 間の三角形がどの順序でも背面カリングされて描画されない → デバッグに時間使うより `PlaneMesh` で確実に動かす方を選択
- **水面 Y = 湖底 + 0.3**(初期)→ 湖の谷が深さ ~5m に対し水面 30cm 上だと、ほぼ全面が地形に隠れる → +4.5m に上げて湖らしい広さの水面に

### Phase 1 全体の総括

- ✅ 1-1 地形(400×400、山 3 + 湖、頂点カラー、HeightMapShape3D 衝突)
- ✅ 1-2 空と昼夜サイクル(84 秒、SkyColor 関数、太陽の動き)
- ✅ 1-3 線路(楕円 Path3D、レール 2 本統合 ArrayMesh、枕木 MultiMesh 157 本)
- ✅ 1-4 雲(18 個、6 球の集合、水平流れ)
- ✅ 1-5 湖(PlaneMesh + water.gdshader、波 + スペキュラ)
- ✅ 1-6 桜の花びら(GPUParticles3D、Player 追従)
- ✅ 派生: 星 12 個(夜のみ visible)、自動スクリーンショット基盤

子供にとって「歩ける + 線路がある + 空が動く + 雲が流れる + 桜が舞う + 湖がある + 夜に星が出る」世界が立ち上がった。次は Phase 2(列車システム、9 編成の新幹線)。

## v0.7.0 — 2026-05-29 — Phase 1-6(桜の花びら)

- `scenes/fx/CherryPetals.tscn` 新規 — `GPUParticles3D` 単体、スクリプトなし。`ParticleProcessMaterial` + `QuadMesh`(0.4×0.4) + `StandardMaterial3D`(UNSHADED、半透明、BILLBOARD_PARTICLES)を sub_resource として埋め込み
- `scenes/Main.tscn` 修正 — Player ノードの **子** として CherryPetals をインスタンス化(Player に追従して常に取り囲む)、Player の頭上 10m に配置

### 主要パラメータ

- `amount = 250`、`lifetime = 6.0`、`preprocess = 3.0`(起動時から既に降っている)
- `emission_shape = BOX`、`emission_box_extents = (15, 0.1, 15)` で Player 中心 30m 四方の薄い天井から発生
- `direction = (0, -1, 0)`、`gravity = (0, -1.5, 0)`、`initial_velocity 0.3〜0.8`
- `angular_velocity ±30°/s` で回転しながら舞う
- `turbulence_enabled = true`、`turbulence_noise_strength = 0.5` で sin/cos の代替(ふわふわ感)
- `scale 0.4〜1.0` 倍 → 16cm〜40cm の花びら(子供視点で見える + 近距離で巨大化しない)
- 色 `#ffc4dd` 薄ピンクベージュ、alpha 0.8(Three.js プロトタイプと同じ)

### 試行錯誤メモ

- 初期 `scale 0.05〜0.15` だと小さすぎて画面に映らない → 0.4〜1.0 に調整
- `scale 0.5〜1.5` だとカメラ至近距離で巨大な花びらが画面いっぱいに → 0.4〜1.0 に縮小
- Player の頭上 8m だと近すぎ、12m 前方 -3m だと遠すぎ → 真上 10m の中間で落ち着く

### 改善さんのフィードバック対応

「カメラ位置を変えられないので雲は影でしか見えない」→ 桜の花びらは Player を取り囲んで降るのでプレイヤー視点でも直接見える。固定カメラの制約を補う演出

次のステップ: Phase 1-5(湖の water シェーダー)で Phase 1 完了予定

## v0.6.0 — 2026-05-29 — Phase 1-2(空・昼夜)+ 1-4(雲)+ 星

- `scripts/world/sky_color.gd` 新規 — `class_name SkyColor`(+ preload 両対応)。時刻 t(0.0〜1.0)から `background(t)` / `ambient(t)` / `ambient_energy(t)` / `sun_color(t)` / `sun_energy(t)` / `sun_position(t)` / `fog_color(t)` を返す純粋関数 7 つ。Three.js プロトタイプの updateTimeOfDay を移植
- `scripts/world/day_night_cycle.gd` 新規 — `class_name DayNightCycle`。1 サイクル 84 秒で時刻を進め、毎フレーム WorldEnvironment(背景・環境光・fog 色)と DirectionalLight(位置・色・強度)を更新。`time_of_day` プロパティに setter を入れて外部から代入したら即反映
- `scripts/world/clouds.gd` + `scenes/world/Clouds.tscn` 新規 — 18 個の雲を seed 7 で固定配置、各雲は 6 個の SphereMesh の集合、UNSHADED マテリアル(時刻に依存しない常に白)。X 方向に水平移動 + 端でワープ
- `scripts/world/stars.gd` + `scenes/world/Stars.tscn` 新規 — 12 個の星を seed 42 で固定配置(地形高さ + 1.8〜2.3m)。夜のみ visible、ゆっくり自転、emission で発光(黄色 #ffe066)
- `scenes/Main.tscn` 修正 — `DayNightCycle` / `Clouds` / `Stars` ノード追加、Stars に DayNightCycle / Terrain への NodePath 接続、不要な Sky/Sky_material SubResource を削除
- `scripts/dev/auto_capture.gd` 拡張 — `MODE = FOUR_TIMES` で 1 回起動から朝(0.25)/昼(0.50)/夕(0.75)/夜(0.95)の 4 枚を順次撮影、`DayNightCycle.paused` を立ててから時刻を直接代入

### 修正したハマりポイント

- **`class_name` は Godot エディタが project をスキャンしないと CLI で認識されない**(SCRIPT ERROR: Identifier "SkyColor" not declared)→ `const SkyColor = preload("res://...")` を併用して両対応
- **昼の地形が白飛びする** → SUN/AMBIENT energy を下げて Three.js より控えめに(`DAY_SUN_ENERGY 0.9`、`DAY_AMBIENT_ENERGY 0.25`)。Phase 1-1 で経験した「Linear tonemap + 強ライト」と同じ症状の再発
- **夜の空が水色のまま**(背景は紫青なのに) → Godot の Environment は fog が遠景の空を上書きする。`SkyColor.fog_color(t) = SkyColor.background(t)` で fog 色も時刻同期させて解決

### Environment 設定の追加変更

- `WorldEnvironment.fog_light_color` を毎フレーム DayNightCycle が時刻に応じて上書き(夜=紫青、夕=オレンジ、昼=空色)

### UX メモ

- 改善さんから「カメラの位置を変えられないので雲はわかりませんが影は見えます」のフィードバック。三人称固定カメラ(CameraRig)では上空が視界に入りにくいため、雲の存在は地面に落ちる影で間接的に感じてもらう設計。本格的な「空を見上げる UI」は Phase 6 以降の検討事項

次のステップ: Web Export + push → Vercel デプロイ → Phase 1-5(水)/ 1-6(桜)へ

## v0.5.0 — 2026-05-29 — Phase 1-1 + 1-3(地形 + 線路)+ 自動スクリーンショット基盤

- `scripts/world/terrain_height.gd` 新規 — `class_name TerrainHeight` 純粋関数群。`compute_height(x, z)`(Three.js heightAt 移植、山 3+湖+二重正弦波+ノイズ)、`compute_vertex_color(h)`(雪山→草原→砂の高さマッピング)
- `scripts/world/terrain.gd` 新規 — ArrayMesh で 121×121 頂点の地形メッシュを動的生成(28800 三角形)、HeightMapShape3D 201×201 サンプルで衝突(scale で実世界 400×400 に展開)、湖 CylinderMesh 配置
- `scenes/world/Terrain.tscn` 新規 — StaticBody3D + TerrainMesh + TerrainCollision + LakeMesh の最小構成、生成はスクリプト
- `scripts/world/railway.gd` 新規 — 楕円(R_X=100, R_Z=78)を 157 点で表現、レール 2 本を 1 つの ArrayMesh に統合(円柱 8 角形断面を Path に押し出し)、枕木 MultiMeshInstance3D(157 個、地形高さに追従)。`static func ellipse_point(t)` / `ellipse_tangent(t)` で Phase 2 の Train から再利用可能に
- `scenes/world/Railway.tscn` 新規 — Node3D + TrackPath (Path3D) + Rails + Ties
- `scenes/Main.tscn` 修正 — 既存平面 Ground を削除、Terrain と Railway を追加、Sun の `directional_shadow_max_distance=100` に縮小(iPad パフォーマンス)
- `scripts/main.gd` 修正 — `_settle_player_on_terrain()` で Player の Y を `TerrainHeight.compute_height(px, pz) + 1.5` に動的補正(地形のくぼみに埋まる/高山スタート事故防止)
- **自動スクリーンショット基盤**: `scripts/dev/auto_capture.gd` + `scenes/dev/AutoCapture.tscn` 新規 — Godot CLI で起動すると 2 秒待ってスクリーンショットを `user://screenshot.png` に保存して終了。視点モードを PLAYER / BIRD / SIDE で切り替え可能。これにより Claude が改善さんに F5 を依頼しなくても見た目を自動確認できるようになった

### 修正したハマりポイント(failure-log 候補)

- **ArrayMesh の三角形インデックスは反時計回り(CCW)が表面**。最初に時計回り順で `(i0, i2, i1)` と書いて地形の平地が完全に消えていた(山の斜面だけ見えた)。`(i0, i1, i2)` `(i1, i3, i2)` に修正
- **WorldEnvironment のデフォルト Filmic tonemap + 強いライト + fog 0.004 で頂点カラーが淡くなりすぎる**。`tonemap_mode=0`(Linear)、`ambient_light_energy=0.3`、`fog_density=0.002` に調整して Three.js 版に近い鮮やかさを回復

### Environment 値の変更

- `ambient_light_energy`: 0.5 → 0.3
- `tonemap_mode`: 2 (Filmic) → 0 (Linear)
- `fog_density`: 0.004 → 0.002

次のステップ: 改善さんが Web Export(ファイル名 `index` を手動入力、Custom HTML Shell を確認)→ git push で Vercel 自動再デプロイ。PC ブラウザ確認後、Phase 1-2(空・昼夜)/ 1-4(雲)/ 1-5(水)/ 1-6(桜)へ。

## v0.4.0 — 2026-05-28 — Phase 0-6(Vercel デプロイ + 本番動作確認)

- `.gitignore` 修正 — `export/` → `export/*` + `!export/web/` に変更(Git の仕様でディレクトリ全体を ignore すると配下を `!` で例外指定できないため、`export/*` 表記に切り替え)。`export/web/` 以外の `export/*`(将来の iOS/Android Export 等)は引き続き除外
- `vercel.json` 修正 — `outputDirectory: "export/web"` を追加(Vercel UI で Output Directory を手動指定する手間を省くため。Project Settings UI を一切触らず Deploy ボタンだけで完結)
- `export/web/` 配下 12 ファイルを git 管理化(`index.html/js/wasm/pck`、audio worklet 2 種、アイコン 3 種、`.import` メタ 3 種)。`index.wasm` 35.7MB(GitHub 100MB 上限内、LFS 不要)
- GitHub Public リポジトリ作成: https://github.com/fanxingjudao-boop/shinkansen-world-godot (`gh repo create --public --source=. --remote=origin --push` で一発)
- 改善さんが Vercel にサインアップ(GitHub 連携、Hobby プラン)→ `shinkansen-world-godot` をインポート → Deploy。初回の Configure Project 画面では `vercel.json` の設定が効いたため Build/Output 設定は触らず
- Vercel が `https://shinkansen-world-godot.vercel.app/` を払い出し、ステータス Ready(緑)。COEP/COOP/CORP ヘッダーと `Content-Type: application/wasm` も `vercel.json` で適用済み
- 初回アクセスで改善さんが 404 NOT_FOUND を踏んだが、Claude Code 側から WebFetch で確認したところ Vercel は `index.html` を正常返却 → エッジ伝播遅延 or ブラウザキャッシュと判断 → シークレットウィンドウで再アクセスして動作確認 OK
- PC ブラウザ(Chrome)での動作確認完了 — ローディング → ゲーム画面、Player 表示、操作可能
- iPad Safari 実機確認は保留(改善さん判断、Phase 1 と並行でいつでも実施可能。URL は変わらず)
- 次のステップ: Phase 1(ワールド構築: 地形・線路・空・水・桜)に着手

## v0.3.0 — 2026-05-28 — Phase 0-5(Web Export ローカル動作確認)

- `web/template.html` を新規作成 — iPad 向け meta タグ、100dvh、パステル色のローディング画面、ひらがな表示
- `vercel.json` を新規作成 — COEP/COOP/CORP ヘッダー(Phase 0-6 のデプロイ用に先行準備)
- `export_presets.cfg` を新規作成 — Web プリセット(Threading: false、Custom HTML Shell 指定、Compatibility 想定)
- `Main.tscn` 修正 — `background_mode` を Sky(2) → Color(1) に変更し、空色 `#7ec8f5` を `background_color` に直接指定(Compatibility レンダラー + Web Export で ProceduralSky が描画されなかったため)
- `Main.tscn` 修正 — `ambient_light_source` を Sky(3) → Color(2) に変更、`ambient_light_energy` を 0.35 → 0.5 に
- `TouchHUD.tscn` 修正 — D-pad の ▲▼◀▶ ボタンの font_size を 32 → 40、「タッチ」を 22 → 28、「ジャンプ」を 20 → 26 に拡大(改善さんから「ボタン文字つぶれ」フィードバック)
- 改善さんが Godot 4.6 で Web Export Templates をダウンロード、Project > Export > Web → `export/web/index.html` に書き出し成功(初回は ファイル名が「しんかんせんワールド.*」になったため Claude Code がリネーム + HTML 内参照置換、2 回目は最初から `index` 指定で出力)
- ローカル HTTP サーバー(`python -m http.server`)で動作確認完了:操作可能・キャラ移動・ボタン入力すべて動作。背景空色とボタン文字も改善後の Export で適用済み
- 次のステップ: Phase 0-6(Vercel または GitHub Pages へのデプロイ)、その後 iPad 実機での動作確認

## v0.2.0 — 2026-05-27 — Phase 0 着手(0-1 〜 0-4)

- ルート直下の重複ドキュメントを削除(正本は `docs/` 配下に統一)
  - 削除: `ARCHITECTURE.md`, `CLAUDE-godot.md`, `GODOT_NOTES.md`, `ROADMAP-godot.md`
- `project.godot` を作成(Compatibility レンダラー、1920×1080 ビューポート、9.8 重力、空色クリアカラー `#7ec8f5`)
- `icon.svg` を新規作成(新幹線+空+太陽+雲のパステル可愛いアイコン、128×128)
- ディレクトリ構造を作成(`scenes/{entities,ui}/`、`scripts/{entities,world,ui}/`、`.gitkeep` で git 追跡)
- `scripts/main.gd`: InputMap を動的登録(WASD/矢印/Space/E/Enter)、エントリーポイント
- `scripts/entities/player.gd`: CharacterBody3D の移動・ジャンプ、ロジック層(`_compute_horizontal_velocity`, `_compute_yaw`)と Godot 操作層を分離(C# 移行配慮)
- `scripts/world/camera_rig.gd`: 三人称追従カメラ、Lerp で滑らか、ロジック層分離
- `scripts/ui/touch_hud.gd`: タッチボタン → InputMap action ブリッジ
- `scenes/entities/Player.tscn`: カプセル体 + 球頭 + 桜色の鼻、Group "player"
- `scenes/ui/TouchHUD.tscn`: 左下 D-pad(72px、空色)+ 右下ジャンプ(96px、桜色)/タッチ(96px、黄色)ボタン、StyleBoxFlat でパステル彩色、ダーク UI 不使用
- `scenes/Main.tscn`: ProceduralSky(空色〜)、太陽(DirectionalLight3D + 標準シャドウ)、120×120 草原(`#7ec850`)、Player インスタンス、CameraRig+Camera3D、UI レイヤー
- 次のステップ: Godot エディタで F5 実行して動作確認 → 改善さんに引き継ぎ → Phase 0-5(Web Export)

## v0.1.0 — 2026-05-27 — Godot 版への切り替え

- Three.js 版プロトタイプから Godot 4 版への移行を決定
- 引き継ぎ資料一式を整備(CLAUDE.md / HANDOFF.md / ARCHITECTURE.md / ROADMAP.md / GODOT_NOTES.md)
- Three.js プロトタイプを `reference/threejs-prototype/` に保管
- 次のステップ: Phase 0(Godot プロジェクト初期化)
